# AIKOQL Storage Engine v2 — Implementation Plan

Source: `docs/AIKOQL_Storage_Engine_V2_Production_Design.md` (2026-09-01). Branch `feature/sorage-engine`, commit per milestone, NO push (user pushes). TDD loop per milestone: PoV → RED → root-cause GREEN → regression → gates (`cargo fmt --all` + `cargo clippy --all-targets --all-features -- -D warnings`).

## Coder point of view (before implementation)

**Verdict: CONFIRM the architecture.** Segmented WAL → memtable → immutable segments → manifest → L0/L1 compaction is the proven shape for exactly the three limits the MVP certification published (unbounded WAL, full replay at open — 3.8 ms/MB / 8.77× peak, whole dataset in RAM). The doc's restraint is right twice over: don't clone RocksDB, and don't touch the kernel's `StorageEngine` semantics. The MVP engine stays the certified production default until v2 earns its own adoption gate.

**Refinements (senior challenges):**

1. **Version history is safe by key layout, not by special-casing.** AIKOQL's version rows are distinct logical keys `(koid, ts)` (KSE-12/15 evidence: 27-byte version-row keys, heads separate). A pure LSM merge (last-sequence-wins per *user key*) therefore preserves history **by construction** — an old version is never an overwrite of the same key. M5's risk collapses to one thing: retention policy (KEEP/DROP/ARCHIVE) for genuinely-obsolete rows (superseded heads, tombstones). Compaction stays key-space-generic; policy is an input, not an engine feature.
2. **Sequence = per-batch, not per-op.** The kernel commits one atomic batch per transaction (QA2-PROP-002). The commit coordinator assigns one monotonic sequence per batch; per-op sequences inside a batch are redundant. Simplifies WAL frames, memtable entries, and internal keys.
3. **P1 refinement: KO-awareness at the trait boundary is impossible and unnecessary.** `StorageEngine` sees opaque bytes. The real win is prefix-range locality (blocks grouped by key range), which captures AIKOQL's head/relo/reli/type layout on any key space — measured on AIKOQL's, implemented generically.
4. **Multi-process lock lands in M2** (the first milestone that writes): one OS lock file, `LockFileEx`/`flock`, ~20 lines, fail-closed. Prevents the two-writer catastrophe before the engine can write anything.
5. **Legacy migration lands in M3** (after bounded recovery works, never before): §23's WAL→segment builder is the direct path; the certified REC-002 redb snapshot remains the fixed-format fallback. Never delete the source WAL before semantic verification.
6. **Platform honesty:** Windows has no portable directory-fsync — CURRENT publication is write-temp → fsync file → rename (NTFS replace is atomic) → best-effort dir fsync, documented as such.

**Crate strategy:** new crate `crates/storage/aikoql-v2` (lib `aikoql_storage_v2`), depends on `aikoql-kernel` (checksums + later the `StorageEngine` trait), zero v1 production changes. v1 remains default; v2 opts in via `AIKOQL_BACKEND=aikoql-v2` once the engine lands (M2+), becomes default only after a v2 adoption matrix (the M7 discipline).

### Perf stabilization (SE2-M8+) — coder PoV on the TDD QA spec (2026-09-02)

Source: `AIKOQL_Storage_Engine_Performance_Stabilization_TDD_QA.md` (downloads). The QA spec is right about the disease and mostly right about the anatomy; one gate-4 conclusion in the adoption verdict is a misread, and two of its milestones presume an engine shape this one does not have. Every claim below verified against code on 2026-09-02.

**§18 answers (Q1–Q5):**

Q1 — W1's dominant cost is ONE positioned 64 KiB block read per cold get (~350–390 µs of the 399 µs p50), plus a sha256-8 checksum over the payload and a whole-block decode into owned `Vec<SegmentEntry>` (~90 allocations at ~1.4 KB rows). Code: `SegmentReader::block_entries` → `read_segment_at` (segment.rs:762-821); measured by the 2026-09-01 adoption run (adoption-decision.md).

Q2 — Yes. `Db::get` iterates every segment newest-first (db.rs:367) — 7 segments at the adoption dataset. The bloom pre-check (M7) skips ~all non-hits and `locate()` is an in-RAM binary search over block ranges — both cheap; the one block read at the hit is not. A free key-range skip (`key < key_min || key > key_max`) is missing but would save little pre-compaction: L0 segments flush in time order, so each spans ~the whole key space. Candidate selection's real lever is compaction to ONE L1 segment, not a catalog (see below).

Q3 — Yes. `block_entries` decodes ALL entries of the block into owned `Vec<SegmentEntry>` (segment.rs:786-811), then linear-finds. No restart points: prefix compression forces decode-from-block-start. Cache hits clone the whole decoded block too (cache.rs:79) — a warm get still allocates ~the whole block. This is QA M2 exactly as specced.

Q4 — Two stacked causes, one a reporting bug. (a) The matrix's 8-writer row clones the FULL 200-batch vec per writer (group_commit.rs:360) — each of the 8 writers wrote all 200 batches: 1600 writes, 200 fsyncs = **8:1 coalescing**, which the "8 × 25 batches" label mislabeled and adoption-decision.md read as "200/200 fsyncs, groups never coalesce". Coalescing works, exactly to the concurrent-writer count. (b) The real structural flaws: `max_wait_duration` is dead time — every writer is ack-blocked during the window, so nothing can arrive to fill it (8-writer row: 15.6 ms/group ≈ 5 ms window + fsync + apply vs Sync's 1.1 ms/batch); and the blocking `write()` caps in-flight batches at 1/writer — no pipelining by design. Gate 4's "throughput not evidenced" verdict stands, but for the reporting bug plus the dead window, not for zero coalescing — re-evidenced in SE2-M13.

Q5 — Instrument first (SE2-M8), then the biggest single lever: 16 KiB default block target + restart-point decode + a compaction trigger (7 segments → 1) + release the state read-lock across the disk read. Expected: W1 399 µs → ~90–120 µs (read size 64→16 KiB ≈ 4×, one segment ≈ 1.5–2×); W5 328 ms → tens of ms (1 segment, 16× fewer cold blocks); warm/hot after the cache-hit fix ≈ 10–20 µs — RocksDB-class warm, inside the QA head gate (20 µs). Risk: block-format v2 work; v1 blocks stay readable (the reader serves both; the Unsupported policy pins the fail-closed direction).

**Where the QA spec is wrong about this engine (brutal):**

1. QA M1 (SegmentCatalog, `segments_considered ≤ 2` over 100 non-overlapping segments) presumes range-partitioned flushes. SE2 flushes memtables in time order — L0 segments overlap ~the whole key space at the adoption dataset. The catalog would add a data structure the layout makes useless. Real candidate selection = (a) the free key-range skip, (b) a level-aware probe order, (c) runtime compaction to one L1. QA M1 is answered by SE2-M9/M10 as implemented, not as specced.
2. QA M6 (context compilation) is a kernel workload, not a storage-engine milestone — the storage crate compiles no contexts. The engine's contribution is the read path; the W7 certification lives in the M14 workload matrix at the kernel layer.
3. QA M3's HeadIndex (KOID → block pointer) is premature: after restart points + small blocks + a decode-winner-only cache-hit path, a head get is a memtable hit or one small block read. A persistent head index adds key-layout-specific machinery for a delta the measurements must first show exists. The head GATE (≤ 20 µs) is kept; the index is not pre-built.
4. Gate honesty: the QA gates (cold point ≤ 100 µs, warm ≤ 50 µs, hot head ≤ 20 µs, fanout 1/10/50 ms, context ≤ 100 µs) are the right operational targets and are reachable. The §26 ≤2×-of-v1 adoption bound is NOT reachable by any of this — v1 is a RAM mirror (6 µs = memory); disk physics. The QA spec correctly never asks for it. Parity with redb/RocksDB is the achievable bar: redb cold 19 µs rides 4 KiB pages; RocksDB point reads are memtable ~1–10 µs, block-cache hit ~5–20 µs, cold ~100–400 µs. Post-M9..M14 SE2 lands in that class while keeping its measured wins (bounded recovery, lowest RSS, smallest disk).

**Two latent defects the QA spec missed** (found while verifying Q1–Q5; both in scope):

1. **Write-stall:** `Db::get` holds the state RwLock read guard across the disk read (db.rs:358-383) — one cold get stalls every writer ~400 µs. Explains part of W8's 838 µs p50. Fix: `Arc<SegmentReader>` in state, clone Arcs under the guard, probe after dropping it (SE2-M10).
2. **Memtable probe allocates two Vecs per get** for its BTreeMap range bounds (memtable.rs:45-46). One alloc with `range(&(key.to_vec(), 0)..)` + take-while covers it (SE2-M10).

**Milestone series (SE2-M8..M14), mapped to the QA doc's M0..M8** — the QA priority order is kept (P0 instrumentation, P1 point lookup, P2 candidate selection, P3 decode/allocations, P4 traversal, P5 scans, P7 group commit, P8 certification). TDD loop per milestone: PoV → RED → root-cause GREEN → regression → gates (`cargo fmt --all` + `cargo clippy --all-targets --all-features -- -D warnings`). The existing 130-test suite is the QA-03 safety net; the crash matrix is untouched.

## Milestones

### SE2-M0 — Format contracts — done (19/19 green, clippy clean)

Deliver: `CURRENT` (magic `AKCV`, format_version u16, manifest_generation u64, sha256-8 over the preceding bytes — fixed 22-byte layout), `MANIFEST-{generation:06}` (magic `AKMV`, format_version, generation, segment records, wal records, trailing sha256-8), atomic publication (temp → fsync → rename), classification of the §20 corruption classes.

Acceptance (the doc's): golden byte-format tests; unknown version fails closed; manifest corruption detected; atomic CURRENT publication tested; existing StorageEngine semantics unchanged (v2 doesn't implement the trait yet — nothing to break; the kernel and v1 suites are the regression).

TDD REDs (M0): `current_golden` (hex fixture round-trip), `current_corrupt` (checksum mismatch → fail closed; unknown version → fail closed), `manifest_golden` (hex fixture round-trip), `manifest_corrupt` (checksum mismatch; generation mismatch vs CURRENT), `publication_atomic` (torn temp write never visible; crash between write and rename leaves the old CURRENT parseable — child-kill harness, KSE-15 pattern).

### SE2-M1 — Immutable segments — done (17/17 new green; 36/36 suite, clippy clean)

Deliver: segment writer/reader (header, target-sized data blocks, index block, bloom, footer; per-block magic/version/type/entry-count/compressed-size/uncompressed-size/checksum; prefix-compressed keys, value len, value, seq, flags PUT/DELETE/VERSION). Published segments never modified — read handle only.

Format deviations from the doc's sketch, each pinned by the python-generated golden fixture: block target is a writer parameter (tests use 256 B for real multi-block coverage); the footer carries a **skeleton** checksum (header + all block headers + index + bloom + footer fields — not the whole file), so open() stays O(block count); data-block payload checksums are validated lazily on the read that touches the block. Structural damage fails at open, payload damage fails on access.

REDs: round-trip golden (byte-exact vs the independent fixture); random lookup across ~30 blocks; prefix/range scan byte-exact; corrupted segment fails closed (10 structural legs at open, payload flip on access, truncation at 8 boundaries, trailing bytes, unknown-but-clean version → Unsupported); immutability (reader mutates no file byte); publish validation (empty / duplicate (key, seq) / zero target → Invalid, never written).

Test-side fixes found by the REDs: the shared `tmp()` tag+pid scheme collided between parallel tests in one binary (per-call counter suffix); index-key ranges were index-payload-relative (must be file-absolute).

### SE2-M2 — Memtable and flush — done (24/24 new green; 60/60 suite, clippy clean)

Deliver: active memtable (BTreeMap, deterministic — the doc's own order), threshold → immutable → flush → segment; WAL v2 frames (magic, format_version, frame type, batch sequence, payload len, payload, checksum); commit pipeline (assign seq → append WAL → durability boundary → apply memtable → ack — the KSE-13 120a order, ported); durability modes Sync/GroupCommit/Async (Sync default, no silent downgrade); OS file lock (`File::try_lock`, second open = `FormatError::Locked`).

Format deviations from the doc's sketch, each pinned by a python-generated golden fixture or a crash test: the frame checksum is the crate's established sha256-8, not a CRC32; sequence is per batch (design refinement 2); the torn-tail resync classifier (KSE-082B shape) landed here because a kill mid-append produces a partial final frame — a decode failure with nothing valid after it truncates, damage followed by a valid frame fails closed.

Implementation decisions: flush is **synchronous** (deterministic correctness over lock-free sophistication — the doc's own call); `rotate()` exposes the visibility window (active → immutable, reads see both, old-or-new never torn). Publication order makes every crash window recoverable: segment files → manifest → CURRENT → WAL truncate — crash before the manifest leaves orphan segments plus the full WAL (replay recovers), before CURRENT the old pair is consistent, after CURRENT the not-yet-truncated WAL replay is idempotent (same (key, seq) → same value, no seq reuse — pinned by `crash_window_replay_is_idempotent`). GroupCommit/Async skip the per-batch fsync; the real group-commit machinery is SE2-M6.

REDs (all green): writes visible during flush; crash before publication recovers from WAL (two child-kill harnesses — exact-1..=50 pin after park, contiguous-prefix pin mid-burst with flushes interleaving); crash after publication does not duplicate state (deterministic crash-window replay pin); memory threshold flushes without an explicit call; Sync default + explicit opt-in for weaker modes; second process fails closed on the lock.

Windows lesson the REDs caught: `FILE_APPEND_DATA` handles cannot `SetEndOfFile` — the WAL opened with `append(true)` made flush's `set_len(0)` fail with access-denied. Fixed by `write(true)` + explicit end-seek (single-writer is enforced anyway by the OS lock and `&mut self`).

### SE2-M3 — Bounded recovery — done (11/11 new green; 71/71 suite, clippy clean)

Deliver: open = CURRENT → manifest → validate referenced segments → open active WAL → replay only the active WAL. Legacy WAL migration (§23 path) + the §20 corruption-class policies (torn tail, complete corruption, missing segment → fail closed; orphan segment → ignored + reported; partial compaction output → never published, invisible by construction). v1 corruption tests ported to v2 frames (the KSE-082B resync classifier verbatim).

Implementation: `orphan_segments(dir, manifest)` scans SEGMENT-*.seg against the manifest — open eprintlns and ignores them (unreferenced data; a later flush may reuse the id, safe because nothing references the orphan). Migration (`migration.rs`) rides v1's own validated envelope parser (`envelope::parse_at` — the only new crate dependency, aikoql-storage) and re-implements just the frozen batch-payload codec (v1's `decode_batch` is private). The §23 pipeline maps 1:1 onto the M2 machinery: decode → `db.write` per batch (segments → manifest → CURRENT by the normal flush path) → drop → reopen → verify every key against the decoded state → only then report done. The source file is opened read-only — never modified (v1's own open would truncate a torn tail in place; the migrator leaves the source byte-for-byte intact) and never deleted.

REDs (all green): `db_recovery` (6) — reopen touches only manifest + active WAL (historical segment bytes pinned byte-identical; a torn tail truncates the WAL to exactly the one unflushed frame); only the active WAL replays (WAL length == one frame after flush+put; replay consumes no sequence); missing segment fails closed; orphan reported (`orphan_segments` pin + reopen Ok + id/state untouched); corrupt manifest fails closed; middle damage + valid tail fails closed (082B at the Db boundary). `migration` (4) — state moves byte-exact across puts/dels/overwrites and survives reopen (cross-checked against v1's own engine); corrupt source fails closed; torn tail migrates everything before it with the source untouched; empty WAL → fresh v2. `recovery_independence` (1) — the doc's Recovery Independence Test: 20 × 512 MiB segments + 100 MiB active WAL, `SE2M3_NIGHTLY=1` strict opt-in, open cost measured against a segment-free control and reported (never asserted) to `artifacts/storage-engine-v2/recovery-independence.md`.

Two real bugs the REDs caught:

1. `Db::get` ignored `FLAG_DELETE` on segment entries, so a flushed tombstone read back as `Some([])` — invisible until a delete crossed a flush. Fixed at the shared read path (segment entries carry flags; the reader is format-generic).
2. `SegmentReader::open` read the **whole file** (`std::fs::read`) despite the M1 design claim of O(block count) opens — the first Recovery Independence run measured 16.3 s open vs 4.1 s control (12.2 s = 10 GiB of reads). The M1 tests never caught it because segments were small. Fixed: open now reads only the skeleton via bounded positioned reads (`FileExt::read_exact_at`/`seek_read` — no shared seek position, so concurrent readers never race), keeps the `File` in the reader, and loads data-block payloads on the read that touches them (first-touch checksum validation unchanged). Every M1 pin (byte-exact goldens, corruption classes, truncation boundaries, lazy payload validation) holds through the refactor — the truncation walk gained the payload-fit check the streamed skip had to make explicit.

### SE2-M4 — Compaction — done (9/9 new green; 80/80 suite, clippy clean)

Deliver: L0 flush segments → merge → one L1 sorted segment (`compaction.rs`: k-way heap merge over `SegmentIter` — O(k) memory, blocks load lazily). Per key only the max-seq entry survives — reads are newest-wins, so the merge is the exact logical state; distinct keys (AIKOQL's (koid, ts) version rows) are preserved by construction. A tombstone winner drops the key entirely (L1 is the bottom level); an all-tombstone merge publishes no segment at all. Pipeline as designed: select inputs → write tmp → validate (reopen the output before the manifest can name it) → fsync → publish manifest → CURRENT → delete obsolete files. `compact()` is **synchronous** — deterministic correctness over a background thread, the doc's own call for flush; an L0-count trigger arrives when measurements justify one. `CompactStats` (segments/entries in/out) pins the merge.

Reader lifetime: the state RwLock read-scope IS the reader lifetime — no per-reader `Arc<Segment>` indirection. Obsolete files are deleted after the state swap while handles are open with share-delete, so Windows marks them delete-pending: any reader still referencing an obsolete segment keeps its data alive (the §15 Arc guarantee, via the OS). That exposed the one real concurrency gap: `DataBlock.validated` was a `Cell<bool>`, making `SegmentReader` (and so `Db`) `!Sync` — now an `AtomicBool` (a benign race re-validates a deterministic checksum twice), so concurrent readers on one segment are safe, as M3's positioned-read comment had always claimed.

Crash matrix (§25 compaction windows): env-gated park points `AIKOQL_V2_COMPACT_PARK` (after_segment / after_manifest / after_current, a no-op unset) + the KSE-15 child-kill harness. Because compaction is state-preserving, every window recovers the SAME logical state — the tests pin both the logical equality and each window's structural fingerprint (old manifest still current + parked L1 orphan; new manifest published but not current; L1 current with obsolete L0 files not yet deleted).

Two real bugs the REDs caught, both in the merge's heap:

1. The heap pushed `Reverse(seq)` — a max-heap then pops the **min** seq first, so the "winner" was the oldest version and newer puts/deletes were dropped as losers. Caught by `compact_crash_after_current_before_deletion` (record_count 32 vs 24 live keys) and the collapse REDs.
2. The same-key drain only consumed versions already in the heap when the winner popped — versions sitting deeper in the **same segment** then popped later as fresh winners and were emitted as duplicate keys (record_count 90 vs 83). Invisible to the simple REDs (their versions sat in different segments, all in the heap); caught by the seeded multi-flush workload. Fix: advance the winner's iterator before draining, so its own older versions fall into the drain.

### SE2-M5 — Version-aware compaction — done (4/4 new green; 84/84 suite, clippy clean)

Deliver: the retention policy interface as a compaction INPUT (`Retention::Keep/Drop/Archive`, `trait RetentionPolicy { fn classify(&self, key: &[u8]) }`, `KeepAll`) + `Db::compact_with(policy)`; `compact()` = `compact_with(&KeepAll)` — M4 behavior byte-identical (the M4 suite is the regression). ARCHIVE rows leave the live key space and are appended to `archive/ARCHIVE-{id:06}.seg` (all versions — the loser drain feeds the archive writer; the archive is published even when the live output is empty, and is never consulted by the live database — readable only directly via `SegmentReader`). `CompactStats` gains `entries_archived`. Per refinement 1, history preservation is by key layout — the milestone proves it rather than implementing machinery; the engine stays key-space-generic, the policy is a caller assertion that rows are genuinely obsolete.

The semantic-equivalence harness (`tests/semantic_equivalence.rs`) mirrors the kernel's real row layout (repository.rs): version rows `ko/<koid 16B><ts u64 BE>` — distinct keys per (koid, ts), so newest-per-key merge preserves history by construction; heads are same-key overwrites; relationships are mirrored relo/reli index rows.

REDs (all green): `semantic_equivalence_keep_all` — the §25 gate: create KO A → update ×100 (interleaved compactions after every 20 updates, oracle-verified after each) → create KO B → supersede (rel pair with `rel_type = "supersedes"`) → relate → unrelate → canonical flush+compact → all logical answers byte-exact, head == the 100th update's head, all 100 version rows survive, supersede lineage preserved, unrelate held (no resurrection), recompaction is a fixed point (entries_in == entries_out == live keys, L1 record_count pinned, single L1 segment level 1, survives reopen); `retention_drop_removes_only_marked_keys` — DROP on A's `ko/` prefix removes exactly the 100 marked version rows (entries_in − entries_out == 100) while heads/type indexes/B's rows/the supersede lineage survive and the unrelate tombstone still holds; `retention_archive_preserves_dropped_rows` — ARCHIVE: the 100 rows leave the live space, land byte-exact in one `ARCHIVE-*.seg` (entry_count 100, every version reads back, nothing else archived), reopen never consults the archive; `tombstone_never_resurrects` — value in L1 + tombstone in L0 merge together, winner tombstone drops both (entries_in 2, out 0, manifest segments empty — the deletion is total).

Documented deviation from the plan's "kernel-level invariants (the KSE-12 battery)": v2 does not implement the `StorageEngine` trait yet, so the literal KSE-12 battery cannot run on it — the gate above models the same invariants at key-space level (temporal soundness = every version row survives; head uniqueness = one head row per KOID, latest wins; derived-set equality = relo ⟺ reli mirror; batch atomicity = each kernel batch is one `db.write` batch; supersede lineage preserved). The literal battery lands in the V2-Adopt gate (KSE-20 conformance), unchanged.

One RED-caught test-design flaw (not an engine bug): the gate pins assumed `compact()` drains the memtable — it doesn't (compaction is segment-only by design). The canonical end state now flushes before the final compact, so "L1 holds exactly the live keys" is pinned against a clean memtable.

### SE2-M6 — Group commit

Done (9/9 new green, 93/93 suite).

Delivered: `DurabilityMode::GroupCommit` runs a committer thread; `Db::writer()` returns a `CommitWriter` handle (Clone, any thread) whose `write()` queues the batch and blocks until the ack. Groups are bounded by `max_batch_ops` (4096) / `max_batch_bytes` (16 MiB) / `max_wait_duration` (ZERO) with exact-fit caps — a batch that would exceed a cap is carried over to lead the next group. One fsync per group; apply happens before the acks (acked == durable AND visible). The whole group window (assign seqs → append frames → fsync → apply) holds one `state` write-lock — the same exclusivity Sync's `write()` gets via `&mut self` — so a flush can never interleave it. Lock order is always state → wal, never nested; the wal lock is never held across a flush. Seq order == submission order == WAL order == apply order == ack order, by construction. `Db::fsync_count()` counts commit fsyncs (Sync: per batch; GroupCommit: per group; flush truncation syncs don't count). Sync/Async paths are unchanged — the M2 pins are the regression, and Sync's WAL bytes are the golden group commit reproduces byte-exactly. Drop drops the queue sender and joins the committer (all writer handles must be dropped first — documented). Crash parks generalized: `crash_park(var, dir, stage)` with `AIKOQL_V2_GROUP_PARK` = after_fsync / after_apply / after_ack (the M4 compaction parks now take the var parameter too).

One RED-caught test-design flaw (not an engine bug): the first three REDs submitted batches sequentially through one writer and pinned per-batch grouping — impossible by construction, because a synchronous submitter's `write()` blocks until its ack, so the queue never holds two batches. Group commit exists for concurrent submitters; the tests now submit from threads (one batch per thread), which is also what makes the child-kill stages meaningful (all 20 batches in the one parked group, not one batch plus 19 never-submitted writes).

Perf matrix: `group_commit_throughput_matrix` under `SE2M6_NIGHTLY=1` strict opt-in — Sync 1 writer / GC 1 writer wait=0 / GC 8 writers wait=5ms, 200 batches × 128-byte values, fsync counts per cell, report-only (never asserts), artifact `artifacts/storage-engine-v2/group-commit.md`.

### SE2-M7 — Cache / bloom filters

Done (4/4 new green, 97/97 suite).

Delivered: `Config.cache_bytes` (default 8 MiB, 0 = off) creates one shared `BlockCache` per Db — every SegmentReader the Db opens consults and feeds it. Keys are (reader_id, block_index) with a per-cache never-reused reader identity: segment ids can be reused after orphan cleanup, so a segment-id key could alias a dead reader's blocks. Capacity is decoded entry bytes, hard-capped with LRU eviction; a block bigger than the cap is simply not cached. Hits return a clone of the validated entries — answers never depend on the cache. Metrics via `Db::cache_stats()`: hits / misses / evictions / bytes (all zeros with the cache off). Segment metadata (index + bloom payloads) already lives in memory per reader since M1 — the M7 gap was repeat data-block reads, which the cache closes.

Bloom: built per segment since M1 (m = 10n, k = 4, double hashing over sha256; false negatives impossible by construction, pinned by the M1 `segment_lookup` "bloom never misses"). M7 wires it into the read path: `Db::get` skips a segment the bloom rejects before probing — answer-preserving (false positives only cost a wasted probe).

Perf matrix: `warm_block_cache_speedup` under `SE2M7_NIGHTLY=1` strict opt-in — cold (cache off) vs warm second pass (64 MiB cache), 2000 keys × 200-byte values, one segment, 2000 random-order gets with answers pinned identical across passes; artifact `artifacts/storage-engine-v2/cache-bloom.md`.

### V2-Adopt — the kernel `StorageEngine` adapter + the adoption matrix

Done (21/21 new green, 130/130 suite).

Delivered: `AikoqlStorageEngineV2` (src/engine.rs) implements the kernel `StorageEngine` trait over the v2 Db — an RwLock around the Db guards the `&mut self` writes (readers share, writers serialize; the Db owns its own locking), puts-before-dels op order (KSE-006), an empty batch is a no-op (KSE-005), snapshot/restore ride the trait defaults (REC-002). `Db::scan(prefix)` — sorted ascending, one entry per key (the newest layer's head; layer order is seq order by construction), tombstones shadow, prefix-seeked via BTreeMap ranges + segment index seeks (never a full-key-space walk), byte-successor prefix bounds with an unbounded fallback so high-byte `ko/<koid 16B>` keys survive (the `b"~"` sentinel convention would have dropped them — pinned by a [0xFF;16] key test).

Conformance: `tests/kse20_backend_conformance.rs` — the six KSE-1 asserts (copied verbatim from v1's shared definition) × memory/redb/aikoql/aikoql-v2, plus a reopen persistence probe per durable backend → `artifacts/storage-engine-v2/conformance.md` (the four backends' divergences are documented capabilities; no accidental semantic divergence).

Adoption matrix: `tests/kse_m7_v2_workloads.rs` re-runs the M7 W1..W8 shapes on v2 vs redb vs v1 (same seed, same ops, `V2ADOPT_NIGHTLY=1` strict opt-in) → `workloads.md`; the §26 probe `v2_gate2_3_dataset_larger_than_ram` pins gates 2+3 (820 KB dataset under a 64 KiB memtable + zero cache, segment-served, byte-exact, survives reopen; cache knobs pinned at both ends). First runs of the SE2-M6/SE2-M7 nightly matrices landed `group-commit.md` + `cache-bloom.md` (both path-fixed — they were one `..` short of the artifacts dir). mcp: `AIKOQL_BACKEND=aikoql-v2` is opt-in in `open_engine` (+ the connectors mirror, fail-closed message updated); the production default is unchanged.

Verdict (`artifacts/storage-engine-v2/adoption-decision.md`): conformance + gates 1-3 PASS with committed evidence; gate 4's throughput half NOT evidenced — Sync-not-weakened IS pinned by the M6 suite, but the shipped SE2-M6 matrix could not be read as evidence either way. 2026-09-02 correction: the matrix's 8-writer row cloned the full 200-batch vec per writer (1600 writes, 200 fsyncs = 8:1 coalescing) while labeling it "8 × 25" — the "groups never coalesce" reading in adoption-decision.md was a misread. Coalescing works to the concurrent-writer count; the wait window is dead time (all writers ack-blocked), and the matrix mislabels batch counts. Re-evidenced honestly in SE2-M13. Gate 5 FAILED the adoption-scale run (`V2ADOPT_NIGHTLY=1`, release, 100K/10K/20K, 2026-09-01): W1 p50 399 µs vs v1 6 µs (72.53×), W2 391 µs vs 6 µs (69.79×) against the ≤2× bound — smoke only looked fine because the 2K dataset fit in cache + page cache. Diagnosis: 7 segments + a 54 MB active WAL ≈ 354 MB working set against the 8 MiB block cache (1:44) → every uniformly-sampled get is one cold 64 KiB block read; v1's RAM mirror pays zero disk at query time by design. **VERDICT: NOT ADOPT — v2 stays OPT-IN (default remains v1)**; v2 is the qualified bounded-memory/bounded-recovery/disk-constrained profile engine (lowest RSS 496.93 MiB, smallest disk 354.36 MiB, ingestion within 1.4× of v1). Remediation ceilings (compaction, smaller blocks, cache-sized-to-working-set) are analyzed in adoption-decision.md — even optimistic arithmetic does not reach the 36× needed for the bound.

### SE2-M8 — Read-path instrumentation truth layer (QA M0)

Deliver: `Db::read_path_stats()` — the QA doc's `ReadPathMetrics` as cumulative atomics: `lookups`, `memtable_lookup_ns`/`memtable_hits`, `segments_considered`/`segments_range_skipped` (0 until the range skip lands)/`segments_bloom_skipped`/`segments_index_searched`, `index_lookup_ns`, `block_cache_lookup_ns`/`block_cache_hits`/`block_cache_misses`, `block_io_ns`/`block_decode_ns`, `blocks_read`/`bytes_read`/`entries_decoded`. `value_decode_ns` folds into `block_decode_ns` — values decode with their entries, a separate counter would be fiction. Stats ride the M7 cache plumbing: Db-opened readers carry an `Arc<Stats>`; readers opened directly (`SegmentReader::open`) carry None — zero overhead in the golden/format suites. Timings only run when stats are attached. SegmentReader-level counters count every block load (scans and compaction included); the Db-level counters count only `Db::get` — the honest split for the W1/W2 diagnosis. M8 changes no algorithm — instrumentation ONLY, per the QA doc's own discipline.

REDs (TC-PERF-0001..0003 mapped): `instrumented_point_lookup_populates_metrics` — fresh Db, 10 flushed segments, one get: baseline all-zero, `lookups == 1`, `segments_considered == 10`, `blocks_read == 1`, `bytes_read > 0`, `entries_decoded >= 1`, `block_io_ns > 0`, `memtable_lookup_ns > 0` (counters move only with real operations — never synthetic); `bloom_skip_evidence` — `segments_bloom_skipped == 9`, `segments_index_searched == 1`, deterministic by construction (1-entry segments, m = 10: the 9 filler keys' bloom probe positions chosen all-below-bit-5, the target with a probe ≥ bit 5 — so the fillers' blooms provably reject the target; the M1 bloom spec pins the probe formula); `cache_hit_skips_physical_io` — same key twice: first read `block_cache_misses >= 1` and `blocks_read == 1`; second read `block_cache_hits >= 1` with `blocks_read` and `bytes_read` UNCHANGED (no physical I/O while cached — the doc's PASS condition).

**GREEN (2026-09-02)**: 3/3 new, full v2 suite green (all binaries, incl. the 3-min M7 matrix). Two REDs were real catches: (1) the initial test built the target into the NEWEST segment — `Db::get` walks newest-first and stops at the hit, so the asserts that pin the full walk must flush the target FIRST; the engine itself was correct, the test's model of the hit path was not; (2) full-suite regression exposed `cleanup_dataset` (added for M7's temp-hygiene commit) failing on Windows: `remove_dir_all` on a regular FILE returns os error 267, and redb + aikoql v1 leave a file at their dataset path (only aikoql-v2 makes a directory; Memory leaves nothing) — cleanup now dispatches on the path type and treats NotFound as done. `SegmentReader::open_with` is `pub(crate)` (the live `Stats` struct stays crate-private — `ReadPathStats` is the public snapshot).

### SE2-M9 — Block format v2: restart points + borrowed decode + 16 KiB default (QA M1+M2)

Deliver: data blocks get restart points (block version field already exists in the 28-byte header — v2 blocks, segment version unchanged; v1 blocks stay readable, a newer build reading older blocks and vice versa fails closed as today). Payload v2: `restart_interval u16 | restart_count u32 | restart offsets u32[] | entries` — a lookup binary-searches the restarts (entry at each restart starts a fresh key) and decodes only the interval slice; no whole-block decode, no per-entry allocations (borrowed entry views; only the winning value is cloned). Default `Config.block_target` 64 KiB → 16 KiB (the measured read is ~6 µs/KiB on the adoption machine → ~96 µs cold; 4 KiB is the documented knob if M14's measurement wants it). The free key-range skip (`key_min`/`key_max` are already in RAM per reader) lands here too — QA M1's candidate-selection intent, the part that is actually useful pre-compaction. Writer emits v2 blocks; new golden fixture pins the v2 block bytes (the M1 fixtures pin v1 — both stay byte-exact, format drift is a visible diff).

REDs: `block_v2_golden` (byte-exact vs independent fixture; v1-block segments still open and read — mixed-version segments fail closed the other way); `block_v2_lookup_bounded` — a 10 000-entry block, one lookup: `entries_decoded` ≤ the restart interval (16), the QA M2 gate; `restart_search_correctness` — random sorted keys × random intervals × 100k lookups vs brute-force decode, 0 mismatches (the QA TC-PERF-0202 shape); `block_v2_allocation_regression` — 10 000 repeated lookups: allocations per lookup bounded (a small constant, the winner clone + scratch) — the QA TC-PERF-0203 shape; `key_range_skip_evidence` — segments whose [key_min, key_max] excludes the target are never bloom-probed: `segments_range_skipped` moves, `segments_considered` unchanged (considered = iterated). Regression: every M1 segment pin (goldens, corruption classes, truncation, bloom never misses) holds on v1 blocks; all 130 + new green.

**GREEN (2026-09-02)**: 10/10 new (5 block_v2 + 1 alloc + 4 read_path_stats), full v2 suite green, gates clean. Implementation notes: (1) `SegmentWriter::new` stays v1 — the M1 golden pins the v1 writer byte-exact; `new_v2` is used by flush and compaction. (2) The v2 golden fixture initially had a real bug — its index payload pointed the data block at offset 82 (header + block header) instead of 54; the M1 golden's index block (same inputs, byte-identical) and a spec-level skeleton re-hash in Python both cross-checked the corrected fixture, so every fixture byte is independently derived. (3) The cache went raw (`Arc<Vec<u8>>` of validated block bytes) here, not in M11 — a v2 hit re-decodes its interval from shared bytes instead of cloning a decoded Vec; this is M11's cache-half done early, M11 keeps only `hot_head_gate` and the alloc pin. (4) `block_v2_future_version_fails_closed` builds its base from the v1 writer (pinned byte-exact by segment_golden.rs) and patches version 1 → 3 — with block + skeleton checksums recomputed test-side it must say Unsupported, with stale checksums Corrupt; version 0 is Corrupt unconditionally, and the index/bloom blocks stay version-1-only (anything else fails closed the same way). (5) The bounded decode validates the whole restart table up front (offsets inside the payload, full keys at restarts, keys strictly increasing) — the binary search over restarts is only sound on a monotone table, and a damaged one must fail closed, not silently miss. (6) The M8 bloom test was rebuilt to survive the range skip: the nine filler segments now carry TWO keys each ("a…" + "z…" bracketing the "m…" target, m = 20, all filler probes < bit 10, target ≥ bit 10) — the exact 9-bloom-skip/1-search split stays deterministic by construction with the range skip live. (7) Alloc budget on a warm lookup: 2 memtable probes (known M10 defect) + restart-keys vec + scratch + winner key + winner value ≈ 6 ≤ the 8-alloc pin.

### SE2-M10 — Candidate selection at the engine level (QA M1 remainder + structure)

Deliver: `Arc<SegmentReader>` in state — `Db::get` clones the arcs under the read guard, drops the guard, THEN probes (kills the write-stall: a cold get no longer stalls writers ~400 µs); memtable probe down to one alloc (`range(&(key.to_vec(), 0)..)` + take-while); an L0-count compaction trigger (≥ 4 L0 segments → `compact_with(&KeepAll)` on the write path) so the steady state is ONE L1 segment + the active L0 — `segments_considered` per get drops from 7 to 1–2 at the adoption dataset, which is the QA M1 bound (`≤ 2`) achieved by compaction, not by a catalog. Level-aware probe order (L0 newest-first, then L1 — already true by construction; pinned).

REDs: `get_does_not_stall_writers_during_disk_read` — a writer thread measures ack latency while another thread hammers cold gets; with the fix the p50 ack latency stays in the fsync class, not fsync + 400 µs (deterministic: the get thread holds no lock across the read — pinned by making the get path parkable? — if a park proves too invasive, the pin is structural: `get` performs zero lock-acquiring calls between state-guard drop and return, asserted via a scoped-tracer in the test build); `compaction_trigger_bounds_candidates` — seeded multi-flush workload: after the trigger, one get considers ≤ 2 segments (the stats pin), and W1/W5-shaped scans see one segment; `semantic_equivalence` + the full M4/M5 suites hold byte-exact (the trigger runs KeepAll — M5 behavior unchanged).

**GREEN (2026-09-02)**: 3/3 new (`candidate_selection`) + the M9 alloc pin tightened (≤8 → ≤5), full v2 suite green, gates clean. REDs measured: the stall test read p50 ack latency 8856 µs under a 96 MiB-per-get getter vs 923 µs control (9.6×) — the fix drops the state guard before the segment probes, so the getter holds NO state lock across the disk reads; the trigger test saw 4 L0 segments and no L1 (segments_considered = 4+) before the trigger — after it, the 4th write compacts to ONE L1 (record_count 4), a 5th key lands in the active L0, and one get considers exactly 2 segments (range skip 1, index search 1); the alloc pin read 6 (2 probe Vecs) before the probe fix — after, 5 ≤ the tightened pin. Implementation notes: (1) `State.segments` is `Arc<Vec<Arc<SegmentReader>>>` — a get clones the OUTER arc under the read guard (1 refcount bump, 0 allocs) and probes bloom/index/disk lock-free; in-flight gets keep their segment list alive across flush/compaction (snapshot semantics by construction). `Arc::make_mut` extends the vec in place when unshared; `compact_with` swaps the whole vec. (2) The trigger (`Config.l0_compact_trigger`, default 4, 0 disables) fires ONLY on the write path — Sync/Async after the apply+flush block, GroupCommit after the ack (the committer flushes before acking, so the L0 count is current); explicit `flush()` is the caller's checkpoint and never triggers. (3) The memtable probe is one alloc: `range((key.to_vec(), 0)..)` + take-while; `.last()` not `next_back()` (TakeWhile's DoubleEndedIterator impl needs an ExactSize inner, which a BTreeMap range is not). `prefix_heads` rides the same shape (1 alloc; explicit `'a` on the prefix — elided lifetimes don't unify with an explicit self lifetime, E0700). (4) Blast radius on the pinned layouts — 5 suites disable the trigger (`cfg.l0_compact_trigger = 0`): db_compact (manual-compaction + segment-1-survives pins), compact_crash (the explicit compact's crash windows), db_crash (flush/WAL recovery pins), kse_m7 gate2_3 (≥2 SEGMENT files), and — caught by the full suite, not the analysis — read_path_stats (the trigger's compaction runs mid-setup and its I/O moves the segment-level counters, violating the counters-move-only-with-real-operations pin; both shared builders disabled). semantic_equivalence deliberately keeps the trigger ON (answer-based, tolerates it — the M5 regression). (5) The stall test's determinism is by construction, not timing margin: 6 segments × 2 bloom-filler keys ("b-a{i}"/"b-z{i}", 8 MiB constant-fill values, block target 1 GiB, cache 0) + an absent target whose 4 bloom probes (m = 20, independent M1 spec) all land on filler bits — every segment's bloom passes, no skip fires, all 6 × 16 MiB blocks read per get; control samples precede the getter.

### SE2-M11 — Cache hot path (QA M3's gate — cache-half landed in M9)

Deliver: the raw block cache landed in SE2-M9 (a v2 hit = restart search + decode of ≤ interval entries + clone of the winner only — the M9 alloc pin covers the TC-PERF-0203 shape). What remains: the hot-head gate and any alloc tightening the M9 pin leaves on the table. Block target interaction: 16 KiB blocks × ~11 rows decode ≈ µs on hit.

REDs: `hot_head_gate` — 100 000 repeated head lookups on a cached head: `SE2M11_NIGHTLY=1` strict opt-in, report-only P50 vs the 20 µs gate with machine spec (the QA TC-PERF-0303 shape — a perf number never gates a test; the report cell is evidence); `cache_never_changes_answers` + the M7 pins hold.

**GREEN (2026-09-02)**: 1/1 new (`hot_head_gate`), M7 pins hold (cache suite 4/4 unchanged), full v2 suite green, gates clean. The gate was unmeasured before this milestone (no artifact) — now measured: **P50 1.2 µs (P95 1.9, P99 2.9) vs the 20 µs gate → PASS** with 16× margin, release build, AMD64 Family 23 Model 24 (8 logical cores), artifact `artifacts/storage-engine-v2/hot-head.md`. Workload honesty pins inside the test: 257 kernel-shaped head rows (`head/` + 16-byte koid) × ~1.4 KB values, one segment at the 16 KiB default (~11 rows/block — the adoption row shape), the target's block warmed once, then 100 000 cached lookups of the same head with the answer pinned byte-exact per lookup; the measurement must BE the cached path or the report lies — cache hits == 100 000 and zero physical block reads during the run are asserts, not report cells. The QA spec's HeadIndex stays unbuilt: the gate passes by 16× without it, so the delta it would close is provably not there — the plan's §"where the QA spec is wrong" #3 verdict is now evidenced, not argued. No alloc tightening needed: the M9 ≤5 pin already green; the measured head get (memtable probe + bloom + index + cache hit + restart decode + winner clone) is inside the gate with 16× headroom.

### SE2-M12 — Merged iterators + prefix traversal (QA M4+M5)

Deliver: a streaming merged iterator over segments for `Db::scan` — one cursor per segment, k-way merge, per-entry lazy decode through the restart path (no whole-block `Vec` materialization per segment per scan, no BTreeMap of every key in the prefix). `scan` gains the stats: `bytes_read`, `entries_decoded`, `blocks_read` — the amplification evidence for W4/W5. Type scans (W5) ride the same iterator; the 1-segment steady state from M10 is what makes them fast — the iterator removes the per-block reallocation.

REDs: `merged_iterator_correctness` — 50 000 randomized cases (segments × overlapping versions × tombstones) vs the reference scan, 0 mismatches (QA TC-PERF-0501); `prefix_isolation` — relationship-shaped dataset, Entity A's 1000 relos: `entries_decoded` ≈ A's rows, unrelated rows decoded ≈ 0 (QA TC-PERF-0402 — the scan does not touch non-matching segments' payloads); `scan_amplification_report` — W5-shaped scan amplification before/after reported (`SE2M12_NIGHTLY=1`); V2-Adopt's `db_scan` suite is the regression (byte-exact scan contract).

**GREEN (2026-09-02)**: 3/3 new, `db_scan` 4/4 byte-exact unchanged, full v2 suite green, gates clean. The RED is genuine and measured: on the old whole-block path `prefix_isolation` decoded **1402 entries for A's 1000 rows** (blocks decode as a unit — the ~350 pre-start rows sharing A's first block and the post-end rows sharing the last decode too); the merged path lands inside [A_rows, A_rows+64] — the restart seek cuts the pre-start decode to ≤ one interval (≤16 entries), the end bound stops within one entry past the range, and versions within a key yield the head only (bodies skipped without a clone). `merged_iterator_correctness` — 50 000 randomized steps (14% tombstones, flush every 200, compact every 800 + the M10 L0 trigger) vs an independent oracle, 0 mismatches, and 34% faster than the old scan on the same workload (100.2 s → 66 s debug — the whole-block `Vec` + prefix-wide `BTreeMap` churn is gone; the 50k count stays per the plan). The merge is a `BinaryHeap` k-way: per-segment lazy cursors (`SegmentScan` — one block in memory, one entry decoded per step, `entries_decoded` counts every decode including skipped versions, the W4/W5 evidence) + owned memtable-head runs collected under the guard; equal keys drain together, max stream index (newest layer) wins, tombstones suppress; no state lock across segment I/O. Amplification measured (release, `SE2M12_NIGHTLY=1`, artifact `artifacts/storage-engine-v2/scan-amplification.md`): W4 and W5 both **1.01× decode amp** (506/504 decoded for 500 rows), warm scan = 0 physical blocks, cold I/O amp 1.38× (W4) / 2.36× (W5). Honest notes: the kernel's `type/<name>/<koid>` shape sorts types contiguous, so W5's isolation question is block-boundary containment, not interleaving (the interleaved reading of the QA spec is wrong); v1 blocks fall back to a sequential walk with start-skipping (the Db writes v2; v1 stays readable); `scan_seek_pos` validates bounds + shared = 0 per probe with the full-table validation staying in `block_get_v2` (checksum covers accidental corruption — same exposure as the legacy whole-block scan path, marked `ponytail:` with the upgrade path); `SegmentReader::scan` stays (M1 golden pins it); the old `segment_scan` helper is deleted.

### SE2-M13 — Real group commit evidence (QA M7)

Deliver: fix the matrix — per-writer batch counts honest (8 writers × 25 = 200 batches total, not 200 each), report cells named `batches_submitted`; default `max_wait_duration` stays ZERO (drain-at-wake is the correct policy — the window is dead time under a blocking API, M6/M8 measurement); document the pipelining ceiling (in-flight = 1/writer by design; coalescing = concurrent-submitter count) with the upgrade path (a non-blocking submit API if a workload ever needs window-filling). Re-run the matrix with `SE2M6_NIGHTLY=1` and regenerate `group-commit.md` with correct cells.

REDs: `deterministic_batch` — 20 concurrent commits behind a barrier: `fsync_count < 20`, strong target ≤ 5 (QA TC-PERF-0701 — deterministic: 20 threads, one batch each, all released together; group size = 20 → 1 fsync); `crash_after_group_fsync` + `commit_ordering` — the existing group_crash suite plus a 100 000-schedule ordering pin (QA TC-PERF-0702/0703); `group_commit_effectiveness` — the corrected matrix: `GroupCommit fsync_count < Sync fsync_count` AND per-batch latency compared honestly (QA TC-PERF-0704 — with wait=0 the window tax is gone; per-batch GC ≈ Sync × group size / (group size − scheduling loss), report-only). The M6 suite is the regression (Sync bytes golden, apply-before-ack).

**GREEN (2026-09-02)**: 3/3 new, M6 suite 5/5 + all three group_crash windows unchanged, full v2 suite green, gates clean. Root cause of the M6 misread, pinned in the report: the old matrix's 8-writer arm ran 200 batches **per writer** (1600 total) while its report cell said "×25 batches" — the 200 fsyncs over 1600 writes were 8:1 coalescing all along, misread as "groups never coalesce"; the committer itself was always correct. `deterministic_batch` (QA TC-PERF-0701): 20 threads released together behind a barrier into the 200 ms window — fsyncs ≤ 5 (the strong pin; the loose one is < 20), all 20 acked batches visible after reopen. `commit_ordering` (QA TC-PERF-0703): 8 persistent writers × 12 500 = **100 000 commit schedules** at wait=0 — per-writer seqs strictly increase in submission order (blocking ack ⇒ ack order = submission order), every seq globally unique (never assigned twice), min 1 / max 100 000 over the distinct set ⇒ exactly 1..=100 000 — nothing lost, no phantom; a sample of the 100 000 survives recovery. Crash windows were already covered by the M6 group_crash suite (after_fsync_before_apply / after_apply_before_ack / after_ack) — kept as the regression, no new crash test needed. Corrected matrix (release, `SE2M6_NIGHTLY=1`, artifact `group-commit.md` regenerated, cells named `batches_submitted` so a row cannot lie again): Sync 1 writer × 200 = 0.80 ms/batch; GroupCommit 1 × 200 wait=0 = 0.82 ms/batch with exactly 200 fsyncs — the in-flight = 1-per-writer ceiling **pinned by an assert** (a single blocking writer cannot coalesce by construction); GroupCommit 8 × 25 wait=0 = **37 fsyncs, 0.23 ms/batch, avg group 5.4** — coalescing at wait=0 is real and comes from concurrent submitters, not from the window (QA TC-PERF-0704: 3.5× the Sync baseline). Policy decided and documented on `Config.max_wait_duration` (SE2-M13 note): the default stays ZERO — under the blocking ack the window is dead time (the M6 wait=5 ms arm measured ~5 ms window tax per group for zero extra coalescing); the pipelining ceiling is in-flight = 1 per writer by design, and the upgrade path if a workload ever needs window-filling is a non-blocking submit API.

### SE2-M14 — Realistic scale certification (QA M8)

Deliver: DS-PERF-M (100K KOs × 5 versions + relationships) and DS-PERF-L (1M × 10) datasets; the QA benchmark matrix (cold/warm/hot × W1/W2/W3/W4/W5/W7/W8) with throughput, P50/P95/P99, CPU, RSS, bytes read, cache hit rate, segments touched, fsync count; the QA gates: cold point ≤ 100 µs, warm ≤ 50 µs, hot head ≤ 20 µs, fanout F=10/100/1000 ≤ 1/10/50 ms, hot context ≤ 100 µs, group commit beats Sync where batching is possible. `SE2M14_NIGHTLY=1` strict opt-in, artifacts under `artifacts/storage-engine-v2/`; every gate row reports the machine spec (the QA doc's rule — never silently relax). The adoption matrix re-runs on the same harness; the §26 ≤2×-of-v1 bound stays out of scope (RAM-vs-disk physics — priced in by the 2026-09-01 verdict), the comparison that matters is redb/RocksDB-class parity on the same machine.

**GREEN (2026-09-03)**: harness 3/3 green at smoke, full v2 suite green (137), gates clean, and the mode-2 nightly (`SE2M14_NIGHTLY=2`, release, 3 h 16 m) green end to end — report `artifacts/storage-engine-v2/scale-certification.md`. Datasets: DS-PERF-M 100K × 5 = 2 702 200 rows (77.5 s seed, 100 002 fsyncs, 157.77 MiB, 1 segment); DS-PERF-L 1M × 10 = 32 002 200 rows (1 750 s seed, 2.12 GiB). All seven QA M8 gate rows PASS on DS-PERF-M with the machine spec per row: cold point 22.0 µs (redb 3.2), warm 4.6 (2.4), hot head 2.3, fanout F=10/100/1000 = 33.8 µs / 314.9 µs / 11 216.4 µs (redb F=1000: 2 403.8), hot context 48.1, group commit cited from SE2-M13 — redb is faster on every comparable M row, reported honestly. Hot head 403 863 ops/s @ P50 2.3 µs (M11's probe was 1.2 µs — the equal-run decode cost, 8.7× under the gate). DS-PERF-L scale check: the hot path stays flat (2.4 µs), cold point 22→30 µs, type scan 632→6 813 µs (~linear in data), F=1000 11.2→20.5 ms. Adoption matrix re-ran on the same harness (child exit 0, `workloads.md` regenerated): 7.5×–23.8× across W1..W8 vs the 09-01 nightly; gate 5 FAIL at W1 6.61× / W2 6.13× v1 (was 72.53× / 69.79× — 11× closer, verdict stays NOT ADOPT); v2 now beats redb on W8 (5.4×) and W6 (2.9×). **Regression found and fixed by this nightly, TDD REDs first** (`block_v2_equal_key_run_restarts` writer-level + `equal_key_versions_survive_flush` engine-level, both fired the exact corruption): v2 restart points landed on every 16th entry unconditionally, but the memtable keys on (key, seq) — the kernel's RMW restatements accumulate versions of one key and a single flush publishes the whole run, which the reader's fail-closed strictly-increasing restart check rejected. The writer now skips restarts on equal keys (intervals over a run exceed 16 — the honest version-lookup cost). Two findings carried in the report for follow-up: seed RSS amplification (DS-PERF-L peak 16.21 GiB vs 2.12 GiB dataset; kse loader 496.93 MiB → 1.35 GiB vs 09-01) and the L seed wall (29 min, fsync-per-batch Sync durability). Machine: windows/x86_64, 8 cores, AMD Ryzen Family 23.

### SE2-M15 — Publish pipeline (streamed segment writer)

Done (2/2 new green, 139/139 suite).

Delivered: `SegmentWriter::publish` streams blocks to the temp file instead of the 5-copy in-memory pipeline (per-block Vec → whole-file buffer → whole-file `Vec<SegmentEntry>`), and returns `(file_size, checksum8)` so compaction measures its own I/O without a second pass. Byte-pins `v2_publish_bytes_pinned` + `v1_multiblock_bytes_pinned` (fixtures v2_publish_pin.hex / v1_multiblock_pin.hex — the v1 multiblock captured from the 00e2270 worktree, so the byte contract is pinned against the ORIGINAL writer, not the rewritten one). Nightly DS-PERF-L re-run: peak RSS 16.21 → 5.10 GiB (−3.2×), seed wall 1750.4 → 1234.2 s (−29.5%) — the M14 follow-up finding, fixed.

### SE2-M16 — Tiered compaction (size-tier merge gates)

Done (3/3 new green, 142/142 suite).

Delivered: `Config.l0_tier_ratio` — size-tier merge gate: L0 compacts only when Σ L0 bytes ≥ Σ L1 bytes / ratio (default 1; 0 = the M10 count-based trigger). REDs: `tiered_trigger_skips_small_l0`, `tiered_skip_reads_walk_unmerged_l0` (reads walk unmerged L0 correctly — no phantom, no lost row), `tiered_ratio_zero_is_count_only`. Nightly DS-PERF-L re-run: seed wall 1234.2 → 1035 s (−16.1%), peak RSS 5.10 → 4.02 GiB (−21.2%), Σ merge I/O 3.04 GB (−76%); merges at L0 4/4/9/17 with the gate held at end. exe-trap lesson: run-1's staircase was a stale pre-M15 exe — exe hash ≠ content, verify by content-grep/build time.

### SE2-M17 — Tiered read path (QA read gates at depth)

Done (2/2 new green, 144/144 suite).

Delivered: the tiered read path certified — `Db::get` walks L0 newest-first then L1, the M7 bloom + M9 range skip absorb fan-out. Probe `SE2M17_READS=1` strict opt-in, release, artifact `tiered-read.md`. QA read gates PASS at depth 17 (16 L0 + 1 L1): cold 27.3 µs ≤ 100, warm 9.5 ≤ 50, hot 3.5 ≤ 20 at L (3.7–5.7× headroom); absent walk 1.7 µs = 15 segments / 0 blocks (metadata traversal provably cheap); type scan 6.23 ms under M14's count-only 6.81 ms. REDs: `tier_depth_answers_match_oracle` (50k oracle, 0 mismatches), `tier_depth_fanout_absorbed` (range-skip + bloom absorb fan-out).

### SE2-M18 — Tier-depth fanout certification

Done (0/0 new — certification only).

Delivered: the full QA read matrix at depth 17 (release, probe-opt-in): fanout warm F=10 59.1 µs / F=100 521 µs / F=1000 21.5 ms; hot context 92.8 µs = 1.08× the thinnest head. Findings reported: F=1000 warm thrashes the 8 MiB cache (the fanout walk re-reads blocks).

### SE2-M19 — V2-adopt re-matrix on post-M18 code

Done (0/0 new — matrix + harness honesty fixes).

Delivered: the adoption-scale matrix re-run on post-M18 code (`V2ADOPT_NIGHTLY=1`, release, 2675 s, exit 0) → workloads.md (2026-09-04) + the adoption-decision.md M19 section. Gate 5 FAILs the ≤2× bound in both page-cache regimes — warm W1 6.53× / W2 6.81× (this run; 7.09×/8.52× on a second warm run the same day), cold 72.53×/69.79× (09-01) — **NOT ADOPT stands**. M15/M16 gains visible: v2 seed wall 294.9 → 221.6 s (≈ v1's 230.2), RSS 496.93 → 428.05 MiB (30% below v1's 611.22), disk 347.99 MiB. Harness honesty fixes: shared `run_date()` civil-date helper (8 lines, no chrono) un-hardcodes the date across the workloads/conformance/scale harnesses; the workloads.md write is guarded by NIGHTLY_ENV so smoke runs stop clobbering the canonical artifact (guard proven by the suite leaving it untouched).

### SE2-M20 — Merge-writer streaming (bounded-memory merge emission)

Done (5/5 new — chunked merge emission, not a true streaming writer).

Delivered: `Config.merge_chunk_bytes` (64 MiB default, 0 = the unbounded pre-M20 shape). `compaction::merge` publishes its output as k bounded segments (live: SEGMENT-{id}.seg, ids from the shared counter in chunk order; archive: archive/ARCHIVE-{id:06}.seg + ARCHIVE-{id:06}-{c}.seg, archive id pulled lazily on first publish); one manifest record per chunk; chunks split on entry granularity in merge order, so they are globally sorted and non-overlapping; every live chunk is reopened for validation; next_segment_id = final counter value. The memory bound is the cap + one entry (entry_bytes = key+value+17); the manifest naming all chunks remains the single atomic commit point, so every pre-M20 crash window is the same window with k files. `SegmentWriter::publish` needed no format change (it mem::takes self.entries — one writer publishes repeatedly). All pre-existing pins held: db_compact segments_out 1/0, compact_crash manifest len 1, semantic_equivalence single ARCHIVE- file — small test datasets stay one chunk at the 64 MiB default. Evidence-drift caveat: M14/M18/M19 artifacts were measured with unbounded merge output; post-M20 re-runs at DS-PERF-M/L emit multiple chunks per merge.

### SE2-M21 — Point-read cost attribution (P0, the read-path recovery program's first milestone)

Done (6/6 new — attribution counters, five unit legs, the adoption-scale probe, and one probe-found engine fix).

Delivered: `lock_wait_ns`, `bloom_probe_ns`, and `get_wall_ns` join ReadPathStats (the whole-get denominator plus the two phases the residual bound needed); unit legs M21-01..05 (the 10% accounting bound over a mixed workload, the memtable/cache-hit/cache-miss mechanism legs, and the merged-segment leg — the compaction output reader must carry the shared cache and stats or its reads go uncounted); the opt-in adoption-scale probe (`SE2M21_ATTRIB=1`, release, ~225 s) publishes P50/P95/P99 attribution for W1/W2/head-row/version-row/memtable/cache-hit to `artifacts/storage-engine-v2/attribution.md`. The probe exposed a real engine defect: compaction reopened its merged output readers BARE (`SegmentReader::open` — no cache, no stats), so every L1 read was an uncached, uncounted raw read — 68% of get wall untimed at adoption scale. Fix: `compaction::merge` (now `pub(crate)`) threads the Db's cache + stats into the reopened live-chunk readers. The unit bound tolerates the documented fixed per-get instrumentation overhead (the phase-boundary timestamps, ~350–420 ns measured) — the probe keeps the PURE ≤10% bound, where a ~36 µs get absorbs the same overhead as <2%.

M22 input (the probe's verdict): warm W1 P50 39.5 µs = engine 35.7 µs (block io 21.4 µs dominant — 25,055 cache misses / 40,000 lookups, 406 MB of block reads per 20K ops, the 8 MiB cache thrashes; decode 3.1; bloom 2.6; index 2.4; cache lookup 1.8; memtable 1.3; lock_wait 0.1; residual 2.7 = 7.6%) + kernel/adapter overhead 3.8 µs. Head-row engine leg 4.8 µs (19,291/20K cache hits — the merged-reader fix also restored the cache for head rows); version-row leg 20.0 µs, io 13.7 µs. Mechanism legs close: memtable hit 0.7 µs, cache hit 1.5 µs. Hypotheses B (synchronization — lock_wait 0.1 µs) and C (metadata traversal — index 2.4 µs) are OUT; the cost is the cache-miss block path, and decode/allocation (3.1 µs) is second.

### SE2-M21..M27 — TDD read-path recovery program (the gate-5 attack)

Source: `AIKOQL_Storage_V2_TDD_Read_Path_Recovery_Plan.md` (downloads; corrected 2026-09-04 against the executed ledger — its original M18..M24 collided with executed M18/M19 and assigned M20, renumbered M21..M27; its §7 W2 target corrected to the gate's own 10 µs; its perf cells verified correct against the 09-04 matrix).

Objective: gate 5 — W1/W2 P50 ≤ 2× v1. Known split: warm W1 37 µs ≈ 27 µs engine get (M17 cold, cache-detached) + ~10 µs kernel/adapter. Evidence already on file: hot head 2.3 µs (M14), warm 9.5 / cold 27.3 / absent 1.7 µs (M17), fanout 59.1 µs + hot context 92.8 µs (M18) — metadata traversal and synchronization are NOT dominant (M10 fixed the read-lock-across-disk stall); the open questions are the ~18 µs cache-miss block path and the ~10 µs kernel leg.

Sequence: **M21 attribution** (done — `attribution.md`: the dominant cost is the cache-miss block path at 21.4 µs of 35.7 µs engine wall (25K misses/20K ops, the 8 MiB cache thrashes); lock_wait 0.1 µs retires hypothesis B, index 2.4 µs retires hypothesis C; the probe also exposed and fixed the compaction readers going uncached/uncounted) → **M22 fix the measured dominant cost** (the proven target: the cache-miss block path; hypothesis A decode/allocation is second at 3.1 µs; acceptance W1 ≥ 25% better, no >10% W6/RSS regression) → **M23/M24 conditional** (head lookup specialization / bounded KO object cache — only if M21 proves their cause; M11's 16× hot-head margin argues against pre-building them) → **scale experiment 1M/10M** (100K fully measured; 1M has M14 QA-gate cells only — DS-PERF-L hot 2.4 / cold 30.1 µs — 10M never; run W1–W8 at both after M22) → **attack the gate** → **M25 relationship batching** (dedup → group by segment → group by block → decode once) → **M26 type scan** (profile first — M17's 6.23 ms at depth 17 vs the 22.6 ms W5 cell already cuts the search space) → **M27 context batch retrieval**.

Discipline carried from the doc: the hypothesis template (Hypothesis / Existing evidence / What will falsify it / Expected contribution / Implementation risk / Correctness risk / Alternatives / Recommendation) before every milestone; keep-or-revert after W1–W8; no indexes without profiling; correctness pins never weakened; adoption gates never redefined to pass; no compaction work while gate 5 fails (M20 delivered on user direction; no further compaction work until the gate is attacked).

### SE2-M25 — Relationship batch read path (done)

Hypothesis: W4 (relationship fan-out) is N per-target kernel gets per op, and each get pays the per-call overheads (state lock, bloom walk, block fetch/checksum, authz guard) — amortizing them across the batch is the win; the decode work stays per key. Existing evidence: 09-05 matrix W4 F=10/100/1000 = 189/961/10508 µs; M21 attribution (block io dominates a warm get; kernel leg = 2 engine gets per get). What would falsify it: a batch leg not beating the per-target loop control at the same fan (the probe runs both shapes). Correctness risk: batch resolution must reproduce the loop exactly (newest PUT wins, DELETE shadows older, dedup doesn't change answers) — pinned by `tests/multi_get.rs` (answers byte-identical to the loop across memtable/segment/tombstone/absent/duplicate mixes; one lookup per unique target; kernel batch byte-exact incl. NotFound fail-fast). Recommendation: ship, keep the harness W4 legs on the batch path, re-run the matrix before any adoption question.

Shipped shape: `StorageEngine::get_many` (defaulted trait method = loop of get — memory/redb/v1/encrypting wrappers untouched; only v2 overrides) → `SegmentReader::get_many` (locate once, group by block, one fetch+checksum per block, per-key bounded interval decode from the held payload) → `Db::get_many` (dedup, one state guard, memtables per unique key, newest-first segments with range+bloom skips) → `ObjectManager::get_many` (heads batch → versions batch, fail-fast NotFound) → `Kernel::get_many` (one auth guard). Honest deviation from the plan's "decode once → resolve many": the block is fetched/validated once and shared; the bounded interval decode (≤16 entries, M9) stays per key — same decode work the loop does. Certification: `v2_m25_relationship_batch` (`SE2M25_NIGHTLY=1`, release) → `artifacts/storage-engine-v2/relationship-batch.md`; the pre-M25 matrix W4 cells are the per-target-loop control.

Measured outcome: batch-vs-loop P50 0.73×–1.13× across the certification runs — the sign flips with run-to-run noise, no measurable batch win at W4's warm fan-out shape, and both suggested targets missed every run (F=100 788–1019 µs vs 700; F=1000 10101–12093 µs vs 6000). The per-leg counters show why: decode is per-key in both shapes by design, and the block-fetch cost the batch amortizes is one the warm 8 MiB block cache already removes — the loop control leg reads zero blocks/op at F=1000, inheriting the batch leg's warmed cache. Hypothesis falsified: the harness W4 legs stay on the per-target loop (the canonical matrix keeps measuring the pre-M25 shape); the batch API remains shipped, correctness-pinned by `tests/multi_get.rs` — M27 (context batch retrieval) may still evaluate it, with this falsification as prior evidence.

### SE2-M26 — Type scan profile (done)

Hypothesis: W5's 4.96× gate-5 cell (27451 µs vs v1's 5534, 09-05 warm) is either the type-index scan shape or kernel-side per-candidate assembly — profile before any index work; the decision tree's thresholds (scan share 15%/40%, kernel residual 30%) were fixed before the run. Existing evidence: M17's 6.23 ms cold scan of 320,022 rows at DS-PERF-L vs the 22.6 ms W5 cell cut the search space. What would falsify a no-index verdict: a scan share ≥ 15%.

Shipped shape (probe-only, 0/0 new — the M18 pattern): `v2_m26_scan_profile` (`SE2M26_NIGHTLY=1`, release) → `artifacts/storage-engine-v2/type-scan-profile.md` — four legs (rotating W5 kernel op, the engine prefix scan alone, kernel gets over the same candidate set, hot-m7_0 ceiling) on the M21/M25 attribution machinery.

Measured outcome: the capture exposed the real W5 shape before any decomposition — the harness's phase-2 `rmv(.., "m7_0")` restates every KO to m7_0, so the index is polluted: m7_0 = 100,000 live candidates, m7_1..99 = 1000 stale phase-1 entries each, rejected by the payload re-check after full decode (stale entries kept by design, kernel.rs:1282); mean candidates per matrix op = 1990. The probe reproduces the matrix cell: rotating p50 27.0 ms vs the 09-05 cell 27.5 ms (1.5%) — the cell IS the 99% stale-type op (1000 candidates → 0 returned); the 1% m7_0 op costs 1.35 s (invisible to p99, 35% of the mean wall — the wall table carries a max column for this). Decomposition of the mean op: engine prefix scan 1.6%, engine point gets 77.8% (2 per candidate), kernel residual 20.6% (4.3 µs/candidate vs 3.2 µs for a plain k.get, +32%). Decision: scan share 1.6% < 15% → no type index / no posting lists / no block summaries — W5 is candidate-bound, and its warm gate-5 cell sits inside the amended ≤8× bound; kernel residual ≤ 30% → no kernel instrumentation. M26 closes as a skip (the M23/M24 pattern); the harness shape is unchanged — matrix cells are the certification reference.

### SE2-M27 — Context compilation batch retrieval (done)

Hypothesis: W7 (context compilation) = 11 head_objects (22 engine gets) + 2 small prefix scans + kernel decode/authz — the get fan-out (11) is nearly W4's F=10, the exact shape M25 falsified; but W7's mix differs from M25's certification legs in one dimension: the matrix draws ids uniform-random with replacement, so each op's rows are scattered (3.7 blocks/op read), while M25's warm repeated-target legs let the block cache amortize everything. The M27 row's gate: show the target mix differs before re-proposing a batch. Existing evidence: 09-05 matrix W7 222 µs vs v1 57 (3.9× — inside the amended ≤8×); M25's falsification (0.73×–1.13×, sign-flipping noise) with the warm-shape confound documented. What would falsify re-proposing the batch: a batch-vs-loop ratio ≥ 0.90 (parity, per the pre-fixed tree) at W7's exact draw sequence.

Shipped shape (probe-only, 0/0 new — the M18/M26 pattern): `v2_m27_context_profile` (`SE2M27_NIGHTLY=1`, release) → `artifacts/storage-engine-v2/context-profile.md` — eight legs on the M21/M25 attribution machinery: the W7 op replaying the harness's EXACT draw sequence (`Xs(SEED ^ 0x27).below(100_000)`, capture-pinned, hubs included when drawn), engine raw scans, kernel gets loop, kernel get_many, kernel history, a second loop/batch pair (the tree's 0.80–0.90 band re-run, built in — M25's ratios flipped sign run-to-run), and a post-W1-W5-thrash matrix-regime replay.

Measured outcome: the probe reproduces the matrix cell — L1 p50 219 µs vs 222 µs (−1.4%) with the exact draw sequence; the first attempt's stride sample understated it by 30% (156 µs) by riding the ring's 64 KiB block locality — the sampling-locality discovery, a general probe lesson: replay the harness's deterministic rng, never substitute a stride. Decomposition of the 257 µs mean op: engine scans 23.6% (61 µs — the tree's block-summary band: scan-shape investigation opens), engine gets 54.8% (141 µs/op), kernel residual 21.6% (≤30% → no kernel instrumentation), history 16.0% (≤30% → no versions-path work). Batch: 0.81×/0.87×/0.85× across three pairs in two runs — direction stable (never parity-or-worse); mechanism pinned: segs/op 14 (batch) vs 116 (loop) — get_many resolves the segment list once, the per-target loop re-walks it per get (the cost M25's warm shape hid). The win is real but modest (~13% of the get sub-shape, ~7% of the op: 222 → ~207 µs) and all three ratios sit in the tree's inconclusive band, below the <0.80 reopen bar. Decision: no new primitives — versions_many/relationships_many would batch per-row decode, and history/outbound_edges are already ONE scan each (M25's analysis holds); get_many stays the existing M25 API as M27 prior evidence; the harness W7 legs stay on the per-target loop (matrix cells are the certification reference — the M25/M26 precedent); adopting get_many in the harness = a user decision at the adoption review (~3.9× → ~3.6× on W7, matrix re-run required).

## Identity & placement architecture (future-ready) — SE2-M29..M39

Source: `artifacts/storage-engine-v2/logical-id-physical-id.md` (2026-09-06). Objective: stable logical identity separated from mutable physical placement (ObjectId → LogicalId → ReplicaId → PhysicalLocation), so replication/sharding/distributed deployment later never forces rewriting storage formats, SDK APIs, identity, compaction, or existing data. Single-node MVP — consensus/networking/replication are explicitly out of scope; only the boundaries that support them later must exist. TDD per milestone; the spec's §31–§41 milestones 1–11 map to SE2-M29..M39 (M28 is the in-flight scale checkpoint — independent; the plain byte API and its W1..W8 matrix stay byte-identical, so the §44 regression gates ride the unchanged matrix).

### Coder PoV (2026-09-06, verified against the executed ledger)

**Verdict: CONFIRM the architecture, with three deviations the spec's own §12/§14/§16 demand and its §54 invites.** The central principle — identity NEVER changes because of flush/compaction/restart/recovery/migration; only PhysicalLocation may — is implementable on this engine without touching a single pinned byte of the existing formats.

**§46 answers (the spec's critical design questions), evidence-first:**

- **Q1 (§19, persist ReplicaId per record?) — YES, fixed u64, as a new DATA BLOCK VERSION (v3).** Options evaluated against the real shapes: A (per-entry) = +8 B/entry — the spec's own default recommendation; B (varint delta) saves ~60% at DS-PERF-L ids (avg ~3 B/entry) but puts branchy decode into the bounded restart-interval path the hot-read gates measure — rejected for the first cut, documented as the follow-up if the M39 memory report shows identity bytes dominate; C (parallel block metadata) = A's bytes plus a second decode structure — strictly worse; D (derive from key) needs a key→identity map, which is per-record identity in a worse container (map keys are 10–100 B rows vs an 8 B field) and impossible generically — the engine sees opaque bytes and must not parse kernel key shapes (the M5 caller-assertion precedent). Evidence for the mechanism: the entry format is already version-gated per data block (`version u16` in the 28-byte block header — the M9 precedent: v1/v2 blocks serve side-by-side, a newer build reading older blocks works, the writer split pinned byte-exact). So identity lands as **block v3** = v2 payload + per-entry `replica_id u64` after `flags`; v1/v2 blocks decode with replica_id = 0. The flush/merge writers emit v3 ONLY when the input carries any identity (any rid ≠ 0) — the pure byte-API engine's bytes do not change at all, and every golden fixture + the M15 publish pins hold untouched.
- **Q2 (bytes/object)** — ObjectId 16 + LogicalId 8 + ReplicaId 8 + placement entry 32 (segment 8 + block 4 + offset 4 + generation 8 + variant) ≈ 72 B of logical fields; in-memory ≈ 48 (identity map) + 32 (replica map) + 56 (placement map) + allocator ≈ ~140–170 B/object estimated — the M38/M39 1M-object measurement (§45, mandatory) replaces this estimate.
- **Q3 (memory-resident?)** — 100K ≈ ~16 MiB, 1M ≈ ~160 MiB: resident is right for the MVP. 10M ≈ 1.6 GB: no — which is why the persistent shape is fixed-width delta records (pageable), not a serialized HashMap.
- **Q4 (metadata > RAM)** — recovery applies per-generation delta logs in order (each file = fixed records + checksum8, atomic publish); a paged reader can stream them — no whole-directory load is architecturally required. The §47 paged design (fixed slots, affected pages rewritten per compaction) is the documented upgrade path and the format does not block it. Not implemented for MVP.
- **Q5 (compaction amplification)** — relocation records = O(distinct replica ids in the merge output), one fixed ~40 B record per relocated replica per publication; metadata write amplification = relocated replicas / live records — measured in M38/M39 (mandatory report cell).

**Deviations from the spec (each §54-challenged with evidence):**

1. **PhysicalLocation must be a variant type, not the §7 flat struct.** §14 demands "placement initially = Memtable" and §16 demands retired-but-historically-resolvable deletion — a flat `{segment, block, offset, generation}` can express neither. MVP shape: `enum Placement { Memtable { generation }, Segment(PhysicalLocation), Retired { generation } }` with `PhysicalLocation { segment_id, block_index, entry_offset, generation }` as the segment arm — the spec's struct survives verbatim; generation lives in every variant so PL-005's older-generation rule is one uniform check. EntryOffset = decoded entry index within the block (the §20 candidate validated against v2's restart-point encoding: byte offsets would address prefix-compressed bytes the reader does not address; the bounded decode addresses entries, so the entry index is the addressable unit).
2. **The three directories are per-generation delta logs** (`IDENTITY-{gen:06}.log`, `REPLICA-{gen:06}.log`, `PLACEMENT-{gen:06}.log` — fixed records, magic + checksum8, atomic publish), not one growing file. Publication order (§23): write log → fsync → manifest → fsync → CURRENT. Every §24 crash state maps onto the engine's ESTABLISHED windows (M0/M4 machinery): state B = the old log is still the named state → old placement authoritative, new segments orphan-GC'd; state C = a delta exists at gen > CURRENT's → ignored + reported at open (the M3 `orphan_segments` pattern, extended to the three log families); state D = both live. One recovery code path for all three directories.
3. **The hot path does not chain three lookups (§11's full chain) — §12/§13 explicitly sanction the flattened form and gate the indirection.** `get_object` = identity → replica resolution (two in-memory map lookups under the existing state read-guard, ~40 ns) then the normal key search; the placement directory is relocation/staleness metadata (written by create/flush/compaction, validated at recovery — never a read hop). The M39 certification measures identity-lookup latency (§43) and the W1..W8 regression gates (≤10%/≤10%/≤15% hot/warm/cold) judge the result; a flattened cache lands only if a gate fails (§13).
4. **Identity allocation rides the WAL.** `create_object()` = one frame `[CreateObject{oid, lid, rid, pgen}]` (new op byte 3; v1 frames replay — the wal_golden fixture untouched), and `put_object` on a new object batches Create+Put in ONE frame — the §14 write path, atomic by construction. ObjectId = `sha256(lid.to_le_bytes())[..16]` — the reserved LogicalId is the unique monotonic source, and it is batch-safe where the originally planned `sha256(next_seq)` is not: two creates in one frame would collide, because a frame carries ONE sequence number for all its ops (the batch is the seq unit — design refinement 2) while each create reserves its own lid; no new dependency; §6.1's future distributed generation = a documented per-node/instance salt. Memtable entries carry `replica_id` (0 = byte API); flush exports the pending identity/replica/placement deltas in the publication window before CURRENT, so WAL truncation never loses them (the segment precedent). The create carries its allocated placement generation (`pgen`) so WAL replay reproduces the EXACT placement record — re-deriving generations at replay would regress a flushed Segment placement back to Memtable in the §24 state-D window.
5. **The kernel adapter stays identity-neutral** (opaque bytes). The identity-carrying surface is the v2 object API: `create_object()` / `put_object` / `delete_object` / `get_object` plus the §27 resolution traits — all exercised by the object API and its tests (no unused abstractions; LocalTopology returns the one local replica).

**§49 forbidden shortcuts — rejected by construction and pinned by tests:** identity never derived from physical location (M29/M32 types); deleted IDs never reused (M30/M31 monotonic-allocator pins); update never allocates a new LogicalId/ReplicaId (M33 MT-002/003/004); compaction never allocates a ReplicaId (M35 CP-001/002/003); placement updates only with crash-recovery tests (M36/M37).

### SE2-M29 — Strong identity types (§31 milestone 1)

Deliver: `identity/` module — `ObjectId([u8; 16])`, `LogicalId(u64)`, `ReplicaId(u64)`, `NodeId(u64)` + `LOCAL_NODE_ID` (the §6/§28.1 four), plus `placement/` with `SegmentId(u64)`, `BlockId(u32)` (the §28.1 six — all Copy/Eq/Hash/Ord newtypes; LogicalId and ReplicaId share a representation but are distinct types, the §6.3 compiler rule). Byte persistence: `to_bytes`/`from_bytes` (LE; ObjectId = 16 raw bytes), Display hex for ObjectId. Ordering documented by derives (u64 ids order by value, ObjectId byte-lexicographic).

REDs: ID-001/002/003 — equality/hash/ordering per type; ID-004 — substitution impossible, a `compile_fail` doc-test (the §6.3 example verbatim: `accepts_replica(logical)` must not compile); ID-005 — all six types persist and recover byte-exactly (round-trip + golden pins), plus the representation pin: LogicalId/ReplicaId/NodeId have identical size 8 while `TypeId` differs — the distinctness is type-level, not size-level.

Acceptance: identity tests PASS; the existing suite untouched (no engine code in this milestone).

**GREEN (2026-09-06)**: `identity_types` 11/11 (equality/hash × all six, ordering, representation pin, byte-exact LE round-trips + goldens, Display/Debug) + the ID-004 `compile_fail` doc-test — RED ran first (unresolved imports), then GREEN; full v2 suite green (no engine code touched — the milestone boundary held); `cargo fmt --all` + clippy `-D warnings` clean. Modules land as `identity/` (ObjectId/LogicalId/ReplicaId/NodeId + `LOCAL_NODE_ID`) and `placement/` (SegmentId/BlockId) in alphabetical position in `lib.rs`.

### SE2-M30 — Identity directory (§32 milestone 2)

Deliver: `Db::create_object()` (WAL op byte 3 `CreateObject` — one frame, one fsync, atomic with a first put when batched), `IDENTITY-{gen:06}.log` / `REPLICA-{gen:06}.log` fixed-record delta logs (magic AKID/AKRP, checksum8, atomic publish; flush exports pending records in the publication window), replay rebuilds both directories (replay of the active WAL + logs ≤ CURRENT's generation; gen > CURRENT = orphan, reported + ignored).

REDs: ID-010..015 — new ObjectId receives a LogicalId; same ObjectId resolves to the same LogicalId; restart preserves mappings; two ObjectIds never share a LogicalId; allocation is monotonic across restart (no reuse by construction); crash between identity-log publish and CURRENT (child-kill) recovers exactly one authoritative mapping — no duplicates, no ambiguity.

**GREEN (2026-09-06)**: `identity_directory` 6/6 — id010 create allocates + resolves (distinct oid/lid, GroupCommit committer arm exercised); id011 stable resolution, unknown ObjectId → None; id012 restart splits the two recovery paths (flushed mapping via the published delta log, unflushed via WAL replay — both resolve); id013 500 creates → 500 distinct lids; id014 monotonic allocator across restart (reopened create gets lid > max); id015 child-kill parks at `after_identity` mid-flush — CURRENT still names gen 1 while IDENTITY-000002.log / REPLICA-000002.log sit on disk, reopen resolves the child's mapping exactly once (no duplicate, no ambiguity), the next create gets lid 2 (the gap is burned, not reused), the orphan log stays reported-and-ignored. RED ran first (E0599: no `create_object`/`resolve_object`), then GREEN. Full v2 suite 43 binaries / 182 passed / 0 failed (the wal_golden fixture held — ops 1/2 are byte-identical); `cargo fmt --all` + clippy `-D warnings` clean (clippy caught both: the group_commit test's local `batch_bytes` needed the CreateObject arm, and a needless borrow in id015).

### SE2-M31 — Local replica directory + topology (§33 milestone 3)

Deliver: `LocalReplicaDirectory` + `LocalTopology` over the replica state: `resolve_local(LogicalId) → Option<ReplicaId>`, `replicas_for(LogicalId) → [ReplicaDescriptor { node, replica }]` (MVP: the one local replica; the future shape is the list of nodes).

REDs: RP-001..005 — one local ReplicaId per LogicalId; stable resolution; restart persistence; ReplicaId never reused; LogicalId/ReplicaId distinct types (the TypeId pin).

**GREEN (2026-09-06)**: `identity_topology` 5/5 — rp001 32 creates resolve 1:1 to distinct local replicas, `replicas_for` answers exactly one descriptor on `LOCAL_NODE_ID`; rp002 resolution stable across independently constructed views, directory and topology surfaces agree; rp003 restart splits both recovery sources (3 flushed via the replica delta log, 2 unflushed via WAL replay — all 5 mappings hold); rp004 allocator resumes past the observed max across restart — nothing handed out comes back; rp005 distinct-type pin (TypeId differs at equal size) + the §9/§10 Send + Sync trait bound holds on both MVP views. RED ran first (E0432: no `identity::topology`), then GREEN. Full v2 suite 44 binaries / 187 passed / 0 failed; `cargo fmt --all` + clippy `-D warnings` clean. Shape: `identity/topology.rs` — the §9.2/§10 traits with the crate's `FormatError` (the spec's `StorageError` name is its own to evolve, §27); the MVP views borrow the live Db (`LocalReplicaDirectory::new(&db)`), delegating to a `pub(crate)` state read — the §27 internal-use surface lands with `get_object` in M33.

### SE2-M32 — Placement directory (§34 milestone 4)

Deliver: `placement/` module — `Placement` enum + `PhysicalLocation` (deviation 1 above), `PLACEMENT-{gen:06}.log` fixed records, apply/recover with generation gating (an older-generation delta never overwrites newer in-memory state — PL-005), structural validation at recovery (segment referenced by the manifest, block/offset within range). Tested at the unit level first — the M0 pattern: format contracts standalone before Db wiring.

REDs: PL-001..005 — ReplicaId resolves to a PhysicalLocation; unknown → None; restart persistence; atomic replace; older generation cannot overwrite newer.

**GREEN (2026-09-06)**: `placement_directory` 9/9 — pl001 Memtable and Segment placements both resolve; pl002 unknown ReplicaId → None; pl003 restart splits both recovery sources (3 flushed via the placement delta log, 2 unflushed via WAL replay — all still `Memtable`, §14); pl004 atomic replacement leaves one entry, no Memtable residue; pl005 the full gate matrix — Stale (older gen ignored), Duplicate (crash-window double-apply no-op), Applied (strictly newer replaces), equal-gen-different fails closed `Corrupt`; plus the M0 format contracts: all-variant round-trip with the 125-byte size pin (18 + 3×33 + 8 — the pin caught a real encode bug, non-Segment records zero-filling 20 bytes instead of the 16 placement-field bytes), damage classes (bad magic / unknown version / truncation / trailing / checksum), publish→recover through the caller's validation closure, and `validate_segment_location` (segment-in-manifest + block/entry range). RED ran first (E0432/E0599: no `placement` module), then GREEN. Full v2 suite 45 binaries / 196 passed / 0 failed (wal_golden held — the CreateObject payload grew 32→40 bytes with `pgen`, ops 1/2 untouched); `cargo fmt --all` + clippy `-D warnings` clean. Shape: `placement/directory.rs` — `Placement`/`PhysicalLocation`/`PlacementRecord`/`ApplyOutcome`, the one `merge_placement` gate every apply path routes through (recovery, live write, GC committer, WAL replay), `PlacementLog` encode/decode/publish + `load_placement_logs`/`orphan_placement_logs`, the unit-testable `PlacementDirectory`, and `PlacementResolver`/`LocalPlacementResolver` (the §9.3 MVP view over the live Db). Db wiring: open() applies logs ≤ CURRENT through the manifest+reader validation closure (orphans reported-and-ignored, the §24 state-C window), allocator resumes past the observed max generation, and flush publishes the placement delta in the publication window (after replica, before the `after_identity` park).

### SE2-M33 — Memtable integration (§35 milestone 5)

Deliver: `MemEntry.replica_id` (0 = byte API); WAL ops 4/5 (`PutObject`/`DeleteObject` carrying rid — v1 frames replay unchanged); `put_object`/`delete_object`/`get_object` (resolve → write/get; update preserves identity by construction — one rid per object, never re-allocated); replay restores memtable identity.

REDs: MT-001..005 — new-object write allocates stable identity; update preserves ObjectId/LogicalId/ReplicaId; read through the identity path returns the right value.

**GREEN (2026-09-06)**: `memtable_identity` 9/9 — mt001 a put to an unknown ObjectId IS the create (§14): the triple allocates, two objects never share identity, re-puts resolve the SAME ids; mt002/003/004 the §15 update path — ObjectId/LogicalId/ReplicaId all stable across updates, delete, and re-write (§16: the tombstone keeps the directory entries); mt005 the §13 read — put/update/delete cycle through `get_object`, plus THE identity-filter pin: a newer byte-API row at the same key answers the plain get but NEVER the object read (§11); mt006 WAL-only restart replays the rid-carrying entries (no flush — replay restores memtable identity); mt007 the fail-closed M34 boundary — `get_object` past flush returns `Unsupported`, never unqualified segment bytes (SE2-M34 lifted this boundary: the row now reads the flushed segment — see the M34 GREEN); mt008 the GC committer applies object ops (acked == visible); mt009 WAL ops 4/5 round-trip + byte-API frames byte-identical. RED ran first (E0432/E0599: no object API), then GREEN. Full v2 suite 46 binaries / 205 passed / 0 failed; `cargo fmt --all` + clippy `-D warnings` clean. Shape: ops 4/5 = `rid u64 | key | (value)` after the op byte — the §17/§18 decision: the entry carries exactly the relocation handle (ReplicaId), the oid→lid map stays in the directory; `put_object` resolves through the §9.1/§9.2 views (`IdentityResolver`/`LocalIdentityDirectory` land with their §27 internal-use moment — the M31 note); a new object's Create+Put ride ONE frame (atomic by construction, deviation 4); concurrent first-puts of the same fresh ObjectId fail closed at the merge gate.

### SE2-M34 — Flush (§36 milestone 6)

Deliver: data block v3 (per-entry `replica_id u64`; v1/v2 read path untouched — the reader dispatches on block version as today; new python-generated v3 golden fixture); flush emits v3 iff the memtable carries identity (byte-API flushes stay byte-identical); `publish_with_anchors` (per-rid max-seq entry location: segment/block/offset) with `publish()` kept as the thin wrapper — zero call-site churn; flush publishes placement deltas (Memtable → Segment) + the M30 identity/replica exports in the publication window before CURRENT.

REDs: FL-001..004 — memtable → segment preserves ReplicaId; placement resolvable after flush; the old Memtable placement is not authoritative after publication; crash before flush publication preserves old state (child-kill).

**GREEN (2026-09-06)**: `block_v3` 7/7 + `flush_identity` 5/5 (FL-001..005) + `memtable_identity` 9/9 (mt007 lifted) — the RED ran first (27 compile errors on exactly the intended surfaces), then GREEN. The python-computed v3 golden matched byte-exact on the writer's FIRST compile — the format spec is self-consistent (header/index/bloom checksums identical to the v2 golden; only the data block differs: version 3, +8 rid bytes per entry, recomputed checksums). Shape: data blocks version 3 = the v2 restart table + `replica_id u64` after each entry's flags byte — the reader dispatches on the block-header version (in_set 1|2|3 for data, 1 for index/bloom), the v1/v2 writers never emit the field, and their goldens (`block_v2_golden`, `segment_golden`, `v2_publish_pin`) held; `publish_with_anchors` returns `(file_size, checksum, Vec<SegmentAnchor{replica_id, seq, block_id, entry_offset}>)` — each rid's max-seq entry location (block-local entry index, the placement directory's resolution surface), `publish()` kept as the thin wrapper writing exactly the same bytes; flush emits v3 iff `memtable.has_identity()` — byte-API flushes stay v2, byte-identical; anchors merge across ALL segments one flush writes (max-seq wins — the multi-immutable edge) and publish as `Placement::Segment` deltas with fresh placement generations (§32: a move IS a new generation) inside the state write-lock window, logged before the manifest names the generation (the §23 order — fl003 pins it across restart); `get_object` reads the segment path newest-first with a rid-filtered bounded decode (≤ one restart interval, 2×16 decoded pinned — the QA M2 bound holds with the filter); `SegmentReader::entry_at(block_id, entry_offset)` (bounds-guarded, Ok(None) past block/segment) serves the anchor; `compact()`/`compact_with()` fail closed `Unsupported` while any placement names a segment — the merge rewrites segments as v2 (rids dropped) and deletes the old files, so a Segment placement would dangle into a Corrupt reopen; SE2-M35 relocates placements through compaction and lifts it (fl005 pins the guard — and caught the ≤1-segment pre-check gap in the test itself; see the M35 GREEN). Full v2 suite 48 binaries / 217 passed / 0 failed; `cargo fmt` + clippy `-D warnings` clean.

### SE2-M35 — Compaction relocation (§37 milestone 7 + §21–25)

Deliver: the relocation protocol — merge winners carry their rid; per-rid anchor = the max-seq surviving entry (a tombstone-dropped rid's placement is Retired — the §16 policy); relocation set → `PLACEMENT-{gen+1}.log` → manifest → CURRENT → obsolete delete; the in-memory placement swap rides the same state write-lock window as the segment swap (CP-006 — old location never returned after publication).

REDs: CP-001..006 — compaction preserves ObjectId/LogicalId/ReplicaId; may change PhysicalLocation; after compaction ReplicaId → new location; old location never returned; generation monotonic across relocations.

**GREEN (2026-09-06)**: `compaction_relocation` 10/10 (CP-001..010) + `flush_identity` 5/5 (fl005 lifted) — the RED ran first (10/10 FAIL on the M34 `Unsupported` guard; cp009 via its child dying on the guard), then GREEN. Shape: the merge carries identity through — `enable_v3` flips each chunk writer the moment an identity-carrying entry arrives (a chunk already published as v2 stays valid: v3 is v2 + the rid field, chunks are independent segments, the reader dispatches per block version — rid-0 rows encode identically either way); the published chunks' anchors aggregate into the `RelocationSet` (per-rid max-seq surviving entry across chunks, `None` when nothing of the replica survived the live key space — tombstone winner, policy drop, or archive — the §16 Retired case); `merge` stays placement-agnostic — it returns the raw material, the driver owns the protocol. The driver: every Segment-placed replica relocates with a FRESH §25 generation (Memtable-placed keep theirs — the next flush moves them); a Segment-placed rid absent from the set fails closed `Corrupt` (the merge saw every entry of every input — never relocate what cannot be proven); the `PlacementLog` publishes at the NEW generation after `state.generation += 1` and BEFORE the manifest (the §23 order, mirroring flush — cp009/cp010 pin the §24 state-C/D windows with the child-kill harness at the new `after_location` park); the in-memory placements swap through `merge_placement` only AFTER CURRENT (CP-006 — old location never returned after publication, old segment files retired). Two recovery hardenings fell out of the REDs: the structural validator moved from per-log load to the SURVIVING map (a relocation supersedes the flush logs' records — a superseded location naming a compacted-away segment is history, not corruption; what remains must name a manifest segment with an in-range block/entry, anything else fails closed) and generation assignment went deterministic (flush anchors and the relocation set sorted by rid — identical workloads allocate identical generations, the crash-pin replays and any future cross-node comparison depend on it). Full v2 suite 49 binaries / 227 passed / 0 failed; `cargo fmt` + clippy `-D warnings` clean.

### SE2-M36 — Crash injection (§38 milestone 8)

Deliver: `AIKOQL_V2_PLACE_PARK` env parks FAIL_AFTER_SEGMENT_WRITE / FAIL_AFTER_SEGMENT_FSYNC / FAIL_AFTER_LOCATION_WRITE / FAIL_AFTER_LOCATION_FSYNC / FAIL_AFTER_MANIFEST_WRITE / FAIL_AFTER_MANIFEST_FSYNC / FAIL_AFTER_PUBLISH (the M4 crash_park generalized); the KSE-15 child-kill harness reopens and validates at each boundary: consistency, all objects readable, no invalid location — the §24 states A–D fingerprint-pinned.

REDs: 7 windows × (reopen → validate → verify objects → verify no invalid locations).

### SE2-M37 — Recovery (§39 milestone 9)

REDs: RC-001..005 — restart restores identity/replica/placement mappings; all committed objects readable; no uncommitted relocation visible (state-C orphan ignored — the §24 deterministic policy).

### SE2-M38 — Relocation stress + memory measurement (§40 milestone 10 + §45)

Deliver: 100,000 objects through updates/flushes/compactions/restarts — ObjectId/LogicalId/ReplicaId pinned unchanged throughout, values correct, PhysicalLocation allowed to change; the mandatory memory report (directory RSS at 1M objects, bytes/object) + the §46 Q2/Q5 measurements → `artifacts/storage-engine-v2/identity-placement.md`.

### SE2-M39 — Randomized oracle + performance certification (§41 milestone 11 + §43/44)

Deliver: random PUT/UPDATE/DELETE/FLUSH/COMPACT/RESTART/CRASH/RECOVER against an independent `BTreeMap<ObjectId, ExpectedState>` oracle (never AIKOQL-internal abstractions — §42); the W1..W8 matrix re-run (byte API unchanged — the ≤10%/≤10%/≤15% gates ride the same harness) + the new identity metrics (identity/replica/placement lookup latency P50, directory RSS, persistence size — §43); gate failures get the §13 evidence pack (benchmark, root cause, optimization, alternative), never a silent pass. Final acceptance = the §52 matrix, with replication NOT NEEDED by design.

## Performance acceptance (§26) — gates for the v2 adoption matrix

Recovery bounded by active WAL (M3 measurement, recovery-independence.md); dataset larger than RAM queryable (V2-Adopt probe); memory limits configurable (M2 Config knobs, V2-Adopt probe); group commit improves concurrent throughput without weakening Sync (M6 — weakening-Sync pinned, throughput half re-evidenced in SE2-M13 after the 2026-09-02 matrix misread correction); KO lookup competitive with the MVP baseline (FAIL at adoption scale in both page-cache regimes — W1 6.53×, W2 6.81× warm on the 2026-09-04 post-M18 re-run, 72.53× / 69.79× cold on 2026-09-01; every measurement is above the ≤2× bound — see the SE2-M19 row above). Measured status lives in `artifacts/storage-engine-v2/adoption-decision.md`.

The 2026-09-02 stabilization program (QA spec, SE2-M8..M14) adds the QA operational gates: cold point P50 ≤ 100 µs, warm ≤ 50 µs, hot head ≤ 20 µs, fanout F=10/100/1000 ≤ 1/10/50 ms, hot context ≤ 100 µs, group commit reduces fsyncs AND beats Sync per-op where batching is possible. Gate numbers are machine-relative — reported with the machine spec, never silently relaxed (the QA doc's own rule); perf numbers remain report cells, never asserts. The ≤2×-of-v1 bound stays out of the stabilization scope: it is a RAM-vs-disk bound, priced in by the 2026-09-01 NOT-ADOPT verdict; the achievable bar is redb/RocksDB-class parity.

## Regression policy

v1 suites untouched; the kernel's `StorageEngine` contract is the compatibility surface. The KSE-20 conformance battery (six asserts × backend) runs on v2 verbatim (tests/kse20_backend_conformance.rs). v1 stays the certified default — v2's adoption matrix ran (2026-09-01) and gate 5 failed at scale, so the default is unchanged on the merits.
