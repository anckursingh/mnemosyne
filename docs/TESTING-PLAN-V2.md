# AIKOQL Storage Engine v2 — Testing Plan

Mirror of `docs/TESTING-PLAN.md` §13.2 for v2. One row per milestone; status flips to ✅ only with real evidence (test names + green counts + artifacts). TDD rules carry over: RED first, never weaken assertions, correctness and performance separated, strict opt-in envs (env-set-but-dead = FAIL), reports end at `artifacts/storage-engine-v2/`, child-kill harness = `current_exe --exact` + env gates (KSE-15 pattern).

| # | Milestone | Status | Evidence |
| --- | --- | --- | --- |
| V2-M0 | Format contracts (design §24 SE2-M0) | ✅ | `crates/storage/aikoql-v2/tests/` — 19/19 green, clippy `-D warnings` clean. `current_golden` 8/8 (22-byte hex fixture `414b435601000700000000000000d34680137cb752ed`; unknown version → `Unsupported` not `Corrupt`; checksum/truncation/overlong/bad-magic → `Corrupt`; missing file → `Io` — a fresh-DB condition, not corruption); `manifest_golden` 8/8 (99-byte hex fixture, segment records with key/seq ranges; count plausibility caps before allocation; generation-vs-CURRENT mismatch → `Corrupt` via `verify_pair`); `publication_atomic` 3/3 (rename consumes the temp — no stray files; 200-publish read-back loop never observes a partial target; child-kill at an arbitrary instant → target complete-or-absent, never torn — KSE-15 harness). Golden bytes are printed fixtures, so a format change is a visible diff |
| V2-M1 | Immutable segments (SE2-M1) | ✅ 17/17 | `segment_golden` (4: byte-exact vs the python-generated fixture, round-trip, reader-never-mutates, publish validation), `segment_lookup` (4: exact head across ~30 blocks, versions seq-desc, missing → None, bloom never misses), `segment_scan` (4: prefix/range/full byte-exact, multi-version head), `segment_corrupt` (5: 10 structural legs fail at open via the skeleton checksum, payload flip fails on access, truncation at 8 boundaries, trailing bytes, unknown-but-clean version → Unsupported) |
| V2-M2 | Memtable + flush (SE2-M2) | ✅ 24/24 | `wal_golden` (8: 51-byte python fixture, round-trip, empty-batch → Invalid, corrupt legs — bad magic/checksum/truncation/impossible length → Corrupt, clean unknown version/type → Unsupported, op-byte flip fails the checksum; replay: torn tail truncates, middle damage + valid tail fails closed, seqs strictly increase), `db_basic` (5: put/get/delete/tombstone, per-batch seqs, reopen recovers unflushed from WAL — Drop never flushes), `db_flush` (5: writes visible during flush via rotate, publication pins — gen bump/segment record fields/WAL truncated, threshold triggers flush, deterministic crash-window replay idempotent, newer memtable shadows segment), `db_crash` (2: child-kill — park → recovered seqs exactly 1..=50; mid-burst with interleaved flushes → contiguous prefix, no acked write lost), `db_lock` (2: second open → Locked, release on drop), `db_modes` (2: Sync default incl. struct-update construction, explicit GroupCommit/Async opt-in) |
| V2-M3 | Bounded recovery (SE2-M3) | ✅ 11/11 | `db_recovery` (6: historical segment bytes pinned byte-identical across reopen — open reads only manifest + active WAL; torn tail truncates WAL to exactly the one unflushed frame; WAL length == one frame after flush+put — only the active WAL replays; missing segment → fail closed; orphan reported via `orphan_segments` + reopen Ok + id/state untouched; corrupt manifest → fail closed; 082B at the Db boundary — middle damage + valid tail fails closed), `migration` (4: v1 WAL → v2 state byte-exact across puts/dels/overwrites, survives reopen, cross-checked against v1's own engine; corrupt source fails closed; torn tail migrates everything before it with source byte-identical — never modified/deleted; empty WAL → fresh v2), `recovery_independence` (1: 20 × 512 MiB segments + 100 MiB active WAL, `SE2M3_NIGHTLY=1` strict opt-in — open 3801 ms vs segment-free control 4323 ms, overhead 0 ms → open cost ≈ active WAL, reported not asserted; artifact `artifacts/storage-engine-v2/recovery-independence.md`). Two real bugs the REDs caught: flushed tombstone read back as `Some([])` (FLAG_DELETE ignored at the read path); first nightly run measured 16.3 s open — `SegmentReader::open` read whole files, now skeleton-only positioned reads (all M1 pins hold) |
| V2-M4 | Compaction (SE2-M4) | ✅ 9/9 | `db_compact` (6: seeded 400-op workload over 120 hot keys with interleaved auto-flushes — every oracle key byte-exact before/after/reopen, L1 record_count == live keys, single L1 segment level 1; tombstone at the bottom drops the key incl. the all-tombstone merge → no output segment, stays dropped across reopen; cross-level and same-level overwrites collapse to the newest (entries_in/out pins); noop without segments; readers-during-compaction — a thread hammering gets while the main thread compacts only ever observes the logical state, plus the obsolete-survives pin: an open reader keeps serving a deleted segment (delete-pending)), `compact_crash` (3: §25 compaction windows via env-gated parks `AIKOQL_V2_COMPACT_PARK` + child-kill — kill after the new segment / after the manifest / after CURRENT: every window recovers the SAME logical state (seq resumes at 121, no acked write lost, no phantom, no duplicate) and the window's structural fingerprint: old manifest + parked L1 orphan; new manifest not yet current; L1 current + obsolete L0 files undeleted). RED-caught bugs: heap `Reverse(seq)` made the OLDEST version win; same-segment versions not yet in the heap leaked as duplicate keys |
| V2-M5 | Version-aware compaction (SE2-M5) | ✅ 4/4 | `semantic_equivalence` (4): kernel-shaped keys mirrored from repository.rs (`ko/<koid 16B><ts BE>` version rows distinct per (koid,ts), `head/`, `relo/`/`reli/` mirrored pairs incl. rel_type "supersedes", `type/`). `semantic_equivalence_keep_all` — the §25 gate: create KO A → update ×100 → create KO B → supersede → relate → unrelate, compactions interleaved, all logical answers byte-exact before == after; head == 100th head; all 100 version rows survive (temporal soundness); supersede lineage preserved; unrelate held; recompaction fixed point (entries_in == entries_out == live, L1 record_count pinned); survives reopen. `retention_drop_removes_only_marked_keys` — DROP on one koid's `ko/` prefix removes exactly the marked rows (entries_in − entries_out == 100); heads/types/other-KO/supersede rows and the unrelate tombstone untouched, across reopen. `retention_archive_preserves_dropped_rows` — ARCHIVE: rows leave the live space, one `ARCHIVE-*.seg` holds all 100 byte-exact (entry_count pin), nothing else archived; reopen never consults archives. `tombstone_never_resurrects` — L1 value + L0 tombstone merge together; winner drops both (in 2/out 0, manifest segments empty). Kernel KSE-12 battery modeled at key level (v2 lacks the StorageEngine trait — the literal battery runs at V2-Adopt via KSE-20). Deviation from plan: gate pins assumed compact drains the memtable — it doesn't by design; canonical end state flushes first |
| V2-M6 | Group commit (SE2-M6) | ✅ 9/9 | `group_commit` (6): `group_commit_coalesces_batches_into_one_fsync` (8 concurrent batches, 200 ms window → 1 group, fsync_count == 1, ack implies apply, survives reopen); `group_commit_respects_max_batch_ops` (10 concurrent one-op batches, cap 4 → exact-fit groups 4/4/2, fsync_count == 3); `group_commit_respects_max_batch_bytes` (ten 100-byte batches, cap 250 → groups of 2, fsync_count == 5); `ack_after_apply_and_orders_hold` (20 batches wait=0: seqs 1..=20 strictly in submission order, just-acked batch already visible, WAL replays 20 frames seqs 1..=20 payload byte-exact); `sync_and_group_commit_wals_are_byte_identical` (deterministic 50-op put/overwrite/delete workload — Sync WAL bytes == GC WAL bytes exactly, states equal); `group_commit_throughput_matrix` (SE2M6_NIGHTLY=1 strict opt-in — Sync 1w / GC 1w wait=0 / GC 8w wait=5ms, fsync counts, correctness assert all acked batches survive reopen, report-only artifact `artifacts/storage-engine-v2/group-commit.md`). `group_crash` (3, child-kill, `AIKOQL_V2_GROUP_PARK` after_fsync/after_apply/after_ack): 20 concurrent batches form one group, kill at each park — every window recovers all 20 keys, WAL replays exactly seqs 1..=20 contiguous (no acked commit lost, no phantom, no gap), sequence resumes at 21. RED-caught test-design flaw: sequential single-writer submits can't coalesce (write blocks on ack) — the REDs now submit concurrently, which is what group commit is for |
| V2-M7 | Cache + bloom (SE2-M7) | ✅ 4/4 | `cache` (4): `cache_is_bounded_and_evicts_under_load` (4 KiB cap over ~45 KiB of distinct blocks, 3 full read rounds — hits+misses == exactly 3×40 lookups, hits > 0, evictions > 0, bytes ≤ cap hard-bound, every answer byte-exact); `cache_never_changes_answers` (300-step seeded put/delete/get workload over 500 keys, interleaved flushes, block target 2048 — cache 0 vs 64 KiB answer vectors byte-identical); `bloom_never_misses_and_false_positive_rate_is_sane` (1000 inserted keys all reported — false negatives forbidden; 10 000 absent keys fpr < 0.25, degenerate-bloom guard; the bloom itself is the M1 construction, M7 wires it into `Db::get` as the skip pre-check); `warm_block_cache_speedup` (SE2M7_NIGHTLY=1 strict opt-in — cold cache-off pass vs warm second pass, 2000 keys × 200-byte values, one segment, random order, answers pinned identical across passes, report-only artifact `artifacts/storage-engine-v2/cache-bloom.md`). Cache keys use a per-cache never-reused reader identity — segment ids can be reused after orphan cleanup, so a segment-id key could serve a dead reader's blocks |
| V2-Adopt | Adoption matrix | ✅ 21/21 new | Adapter + `Db::scan` (`engine` 8: the six KSE-1 asserts granular through `AikoqlStorageEngineV2`, reopen serves durable state, last-put-wins atomic batch; `db_scan` 4: newest-wins layer merge, prefix bounds + sorted + high-byte `ko/` keys, versions collapse to head across 5 segments, survives compact). Conformance battery (`kse20_backend_conformance` 1: memory/redb/aikoql/aikoql-v2 all 6/6 + reopen probes, artifact `conformance.md`). W1..W8 matrix + §26 probe (`kse_m7_v2_workloads` 3: `v2_m7_workloads` matrix, `v2_gate2_3_dataset_larger_than_ram` — 820 KB under 64 KiB memtable + zero cache, segment-served byte-exact, survives reopen, cache knobs pinned; loader child; `V2ADOPT_NIGHTLY=1` strict opt-in; artifact `workloads.md`). First runs of the M6/M7 nightlies (release): `group-commit.md` (GC 1w ≈ Sync, 8w row labeled "×25 batches" but each writer wrote all 200 — 1600 writes, 200 fsyncs = 8:1 coalescing; misread as "groups never coalesce" in adoption-decision.md, corrected 2026-09-02, re-evidenced in SE2-M13), `cache-bloom.md` (warm 4.5× cold) — both path-fixed to `artifacts/storage-engine-v2/` (were one `..` short). mcp wiring: `AIKOQL_BACKEND=aikoql-v2` opt-in in `open_engine` + connectors mirror + fail-closed message; default stays v1. Verdict `artifacts/storage-engine-v2/adoption-decision.md`: **NOT ADOPT — v2 stays OPT-IN (default remains v1)**. The adoption-scale nightly ran 2026-09-01 (`V2ADOPT_NIGHTLY=1`, release, 100K/10K/20K, 3354.97s): conformance + gates 1-3 PASS, gate 4 throughput not evidenced (matrix misread — coalescing works, window is dead time), gate 5 **FAIL** — W1 p50 399 µs vs v1 6 µs (72.53×), W2 391 vs 6 µs (69.79×) against the ≤2× bound; smoke had looked fine (1.15×/1.49×) only because the 2K dataset fit in cache + page cache. Diagnosis (7 segments + 54 MB WAL ≈ 354 MB working set vs 8 MiB cache → every get is one cold 64 KiB block read; v1's RAM mirror pays 0 disk by design) + remediation ceilings in adoption-decision.md; v2 keeps its measured wins — lowest RSS (496.93 MiB), smallest disk (354.36 MiB), ingestion within 1.4× of v1 — as the qualified bounded-memory/bounded-recovery profile engine. 2026-09-02: perf stabilization program (QA spec SE2-M8..M14) starts below |
| SE2-M8 | Read-path instrumentation (QA M0) | ✅ 3/3 new | `read_path_stats` (3): `instrumented_point_lookup_populates_metrics` (fresh Db, 10 flushed segments, one get — baseline all-zero, lookups == 1, segments_considered == 10, blocks_read == 1, bytes_read > 0, entries_decoded ≥ 1, block_io_ns > 0, memtable_lookup_ns > 0 — counters move only with real operations); `bloom_skip_evidence` (segments_bloom_skipped == 9, segments_index_searched == 1 — deterministic by construction: 1-entry segments, m = 10, the 9 filler keys' probe positions all < bit 5, the target probes ≥ bit 5, so the fillers' blooms provably reject the target; target flushed FIRST so get walks the 9 newer segments before the hit); `cache_hit_skips_physical_io` (first read block_cache_misses ≥ 1 + blocks_read == 1; second read block_cache_hits ≥ 1 with blocks_read/bytes_read UNCHANGED — no physical I/O while cached). Instrumentation only — no algorithm change; stats ride the M7 cache plumbing (None → zero overhead); SegmentReader counters count every block load (scans/compaction included), Db counters count only `Db::get`. Regression fix on the way (in `kse_m7_v2_workloads`): `cleanup_dataset` dispatched on path type — redb/aikoql v1 leave a FILE (remove_dir_all fails 267 on Windows), aikoql-v2 a dir, Memory nothing (NotFound — idempotent); full v2 suite green incl. the 3-min M7 matrix |
| SE2-M9 | Block format v2: restart points + borrowed decode + 16 KiB default (QA M1+M2) | ✅ 10/10 new | `block_v2` (5): `block_v2_golden` (byte-exact vs the python-generated fixture — v2 payload `restart_interval u16 \| restart_count u32 \| restart offsets u32[] \| entries`, restart entries shared=0; only the data block differs from the M1 v1 golden: version 2 + table, header/index/bloom/footer byte-identical construction; the fixture's index offset was corrected 82 → 54 cross-checked against the v1 golden's identical index block and a spec-skeleton re-hash); `block_v2_lookup_bounded` (10 000-entry single block, last even key present + odd absent — lookups == 2, blocks_read == 1 (second hits the cache), entries_decoded ≤ 32 = 2 intervals, the QA M2 gate); `restart_search_correctness` (1000 random 16-byte sorted keys, 100k lookups — 50% present / 50% absent random — vs brute-force HashMap, 0 mismatches); `block_v2_scan_matches_v1` (201 entries incl. every-10th old version + tombstone through both writers: get/versions/scan/iter byte-identical); `block_v2_future_version_fails_closed` (v1 base from the pinned writer, version patched 1 → 3, block + skeleton checksums recomputed test-side → Unsupported; stale checksums → Corrupt; version 0 unconditionally Corrupt; index/bloom stay version-1-only). `block_v2_alloc` (1, own binary — process-wide counting GlobalAlloc): 10 000 warm lookups on one cached 10 000-entry block, allocations per lookup ≤ 8 (winner key+value clones, restart-keys vec, scratch, Arc clone, 2 known memtable probes). `read_path_stats` (4): M8 trio rebuilt to survive the range skip — `bloom_skip_evidence` now uses 9 TWO-key filler segments bracketing the target ("a…"/"z…" vs "m…", m = 20, all filler probes < bit 10, target ≥ bit 10: the exact 9-skip/1-search split stays deterministic with the range skip live and range_skipped == 0); new `key_range_skip_evidence` (9 newer single-key "f{i}-x" segments vs "m…" target — range_skipped == 9, bloom_skipped == 0, considered == 10, searched == 1); cache/io pins unchanged. Regression: full v2 suite green incl. the 3-min M7 matrix; v1 golden suite pins the v1 writer byte-exact (writer split: `new` = v1, `new_v2` = flush + compaction) |
| SE2-M10 | Candidate selection: Arc segments, lock-free-read, compaction trigger (QA M1) | ✅ 3/3 new | `candidate_selection` (3): `get_does_not_stall_writers_during_disk_read` (GroupCommit writer × 20 submits vs a getter hammering a cold 96 MiB-per-get workload — 6 segments × 2 filler keys with 8 MiB constant-fill values, cache 0, block target 1 GiB; control p50 first, then under-load p50 must stay within control + 3 ms; RED measured 8856 µs vs 923 µs control — GREEN by construction: the getter clones the segment Arc under the read guard and probes bloom/index/disk with NO state lock across the reads; ≥ 10 getter completions and answer-correctness pinned); `compaction_trigger_bounds_candidates` (memtable 16 → every put flushes; the 4th write leaves 4 L0 → trigger compacts to ONE L1 record_count 4; a 5th key lands in the active L0; one get: lookups 1, segments_considered == 2, range_skipped == 1, index_searched == 1 — the QA M1 ≤2 bound by compaction, not a catalog; manifest [L1, L0]; scan byte-exact vs the 5 keys; level-aware probe order = newest-first, pinned); `explicit_flush_never_triggers_compaction` (memtable usize::MAX, 4× put+flush → 4 L0 segments, never compacts — flush is the caller's checkpoint). `block_v2_alloc` pin tightened ≤8 → ≤5 (the one-alloc memtable probe: `range((key.to_vec(), 0)..)` + take-while; RED read 6 with the two bound-Vec probe). Trigger default 4 (`Config.l0_compact_trigger`, 0 disables); blast radius: db_compact, compact_crash, db_crash, kse_m7 gate2_3 + read_path_stats (full-suite catch — the trigger's mid-setup compaction I/O violates its counters-move-only-with-real-operations pin) disable it in their pinned scenarios; semantic_equivalence keeps it ON as the M5 regression. Full v2 suite green |
| SE2-M11 | Cache hot path: raw-payload cache + decode-winner-only (QA M3 gate) | ✅ 1/1 new | `hot_head_gate` (own binary, `SE2M11_NIGHTLY=1` strict opt-in, release): 257 kernel-shaped head rows (`head/` + 16-byte koid) × ~1.4 KB values, one segment at the 16 KiB default (~11 rows/block), target block warmed once, then 100 000 cached lookups of the same head — answer pinned byte-exact per lookup, cache hits == 100 000 and zero physical block reads during the run as ASSERTS (the measurement must be the cached path); P50/P95/P99 report cells vs the 20 µs QA M3 gate → artifact `artifacts/storage-engine-v2/hot-head.md`: **P50 1.2 µs, P95 1.9, P99 2.9 — PASS** (16× margin, AMD64 Family 23 Model 24, 8 cores). HeadIndex stays unbuilt — the gate passes by 16× without it. M7 pins hold (cache suite 4/4 unchanged); full v2 suite green |
| SE2-M12 | Merged iterators + prefix traversal (QA M4+M5) | ✅ 3/3 new | `merged_iterator_correctness` (50 000 randomized steps vs an independent oracle, 0 mismatches); `prefix_isolation` (RED measured: 1402 decoded for 1000 rows on the whole-block path → merged path inside [A_rows, A_rows+64]); `scan_amplification_report` (SE2M12_NIGHTLY=1 release → artifact `scan-amplification.md`: 1.01× decode amp both shapes, warm scan 0 physical blocks); `db_scan` 4/4 byte-exact unchanged; full v2 suite green |
| SE2-M13 | Real group commit evidence (QA M7) | ✅ 3/3 new | `deterministic_batch` (20 concurrent single-op batches released together behind a barrier into the 200 ms window — fsyncs < 20 loose pin, ≤ 5 strong pin, all 20 acked batches visible after reopen); `commit_ordering` (8 persistent writers × 12 500 = 100 000 schedules at wait=0 — per-writer seqs strictly increase in submission order, every seq globally unique, distinct set min 1 / max 100 000 ⇒ exactly 1..=100 000, sample survives recovery); `group_commit_effectiveness` (`SE2M6_NIGHTLY=1` strict opt-in, release → artifact `group-commit.md` regenerated: Sync 1×200 = 0.80 ms/batch; GC 1×200 wait=0 = exactly 200 fsyncs — the in-flight = 1 ceiling pinned by assert; GC 8×25 wait=0 = 37 fsyncs, 0.23 ms/batch, avg group 5.4 — coalescing at wait=0 comes from concurrent submitters). Root cause of the M6 misread: the old matrix's 8-writer arm ran 200 batches per writer (1600) but the report cell said ×25 — the committer was always correct. Crash windows already covered by the M6 group_crash suite (kept as regression). Default `max_wait_duration` stays ZERO — pipelining ceiling + non-blocking-submit upgrade path documented on the Config field. Full v2 suite green |
| SE2-M14 | Realistic scale certification (QA M8) | ✅ 3/3 new + 2 regression REDs | `scale_certification` (`SE2M14_NIGHTLY=2` strict opt-in, release, 3 h 16 m → artifact `scale-certification.md`): DS-PERF-M 100K × 5 (2.70M rows) + DS-PERF-L 1M × 10 (32.0M rows); all seven QA M8 gate rows PASS on DS-PERF-M (cold 22.0 / warm 4.6 / hot head 2.3 / fanout 33.8·314.9·11 216 µs / hot context 48.1, redb parity rows honest, machine spec per row); L scale check — hot path flat, cold 22→30 µs, type scan ~linear; adoption matrix re-ran on the same harness (child exit 0): 7.5×–23.8× vs the 09-01 nightly, gate 5 FAIL at 6.61×/6.13× v1 (verdict stays NOT ADOPT). Regression the nightly exposed, TDD: v2 restart points on equal-key runs (memtable keeps every (key, seq) version — one flush publishes the run) — `block_v2_equal_key_run_restarts` + `equal_key_versions_survive_flush` fired the exact corruption, writer now skips restarts on equal keys. Findings for follow-up: seed RSS amplification (L peak 16.21 GiB vs 2.12 GiB dataset) + L seed wall. Full v2 suite green (137) |
| SE2-M15 | Publish pipeline: streamed segment writer | ✅ 2/2 new | `segment_golden` pins `v2_publish_bytes_pinned` + `v1_multiblock_bytes_pinned` (hex fixtures v2_publish_pin.hex/v1_multiblock_pin.hex — byte-pinned against the ORIGINAL writer, v1 multiblock captured from the 00e2270 worktree, so the rewrite can't drift the format). Publish streams blocks instead of the 5-copy in-memory pipeline and returns (file_size, checksum8). Nightly DS-PERF-L re-run: peak RSS 16.21→5.10 GiB (−3.2×), seed wall 1750.4→1234.2 s (−29.5%) |
| SE2-M16 | Tiered compaction (size-tier merge gates) | ✅ 3/3 new | `db_tiered_compact` (3): `tiered_trigger_skips_small_l0`, `tiered_skip_reads_walk_unmerged_l0`, `tiered_ratio_zero_is_count_only` — size-tier gate: L0 merges only when Σ L0 bytes ≥ Σ L1 / ratio (default 1; 0 = the M10 count-based trigger). Nightly DS-PERF-L re-run: seed wall 1234.2→1035 s (−16.1%), peak RSS 5.10→4.02 GiB (−21.2%), Σ merge I/O 3.04 GB (−76%); merges at L0 4/4/9/17 with the gate held at end. exe-trap: run-1's staircase was a stale pre-M15 exe (exe hash ≠ content — verify by content-grep/build time) |
| SE2-M17 | Tiered read path (QA read gates at depth) | ✅ 2/2 new | `db_tiered_read` (2): `tier_depth_answers_match_oracle` (50k oracle at depth 17, 0 mismatches), `tier_depth_fanout_absorbed` (range-skip + bloom absorb fan-out). Probe `SE2M17_READS=1` strict opt-in, release, artifact `tiered-read.md`: QA read gates PASS at depth 17 (16 L0 + 1 L1) — cold 27.3 µs ≤ 100, warm 9.5 ≤ 50, hot 3.5 ≤ 20 at L (3.7–5.7× headroom); absent walk 1.7 µs = 15 segments / 0 blocks; type scan 6.23 ms under M14's count-only 6.81 ms |
| SE2-M18 | Tier-depth fanout certification | ✅ 0/0 new | Certification only, no code change. Full QA read matrix at depth 17 (release, probe-opt-in): fanout warm F=10 59.1 µs / F=100 521 µs / F=1000 21.5 ms; hot context 92.8 µs = 1.08× the thinnest head; absent walk 1.7 µs (metadata traversal provably not dominant). F=1000 warm thrashes the 8 MiB cache — reported, not asserted |
| SE2-M19 | V2-adopt re-matrix on post-M18 code | ✅ 0/0 new | `V2ADOPT_NIGHTLY=1` re-run (2675 s, exit 0) → `workloads.md` regenerated (Date: 2026-09-04) + `adoption-decision.md` M19 section. Gate 5 FAILs the ≤2× bound in BOTH page-cache regimes: warm W1 37 µs = 6.53× / W2 36 µs = 6.81× (a second warm run: 7.09×/8.52× — variance inside the regime); cold (09-01) 72.53×/69.79×. M15/M16 gains visible: v2 seed wall 294.9→221.6 s (≈ v1's 230.2), RSS 496.93→428.05 MiB (30% below v1's 611.22), disk 347.99 MiB. Harness honesty fixes: shared `run_date()` civil-date helper (8 lines, no chrono); workloads.md write guarded by NIGHTLY_ENV — smoke runs no longer clobber the canonical artifact (guard proven) |
| SE2-M20 | Merge-writer streaming (bounded-memory merge emission) | ✅ 5/5 new | `merge_chunk` (5): `merge_chunks_respect_chunk_cap` (512-byte cap over ~12 KiB of rows splits ≥ 2 chunks; one file per chunk SEGMENT-{l0+1..=l0+k}, chunk key ranges non-overlapping, Σ entry_count == 100, every key byte-exact, post-compact flush takes id l0+k+1); `merge_chunks_answers_parity_with_unbounded` (same 200-op xorshift workload, chunk 0 vs 512 — scan byte-identical, entries_out equal); `merge_chunks_archive_splits_across_chunks` (ArchiveA, 200+200 rows → ≥ 2 ARCHIVE- files, Σ == 200, live space exact); crash pair via child-kill harness (32-byte cap, memtable 512): `merge_chunks_crash_after_segment_recovers` (old manifest all-L0, k ≥ 2 orphans, verify() = 121-seq resume + id-reuse flush + reopen), `merge_chunks_crash_after_manifest_recovers` (parked manifest: ≥ 2 consecutive-id L1 records, non-overlapping ranges, Σ record_count == live). The manifest naming all chunks stays the single atomic commit point — pre-M20 crash windows are the same windows with k files. Full v2 suite green (149). CAVEAT: M14/M18/M19 artifacts were measured with unbounded merge output; post-M20 re-runs at DS-PERF-M/L emit multiple chunks per merge (segments_considered grows) |
| SE2-M21 | Point read cost attribution (P0) | ✅ 6/6 new | `attribution` (5): M21-01 mixed-path accounting bound (residual beyond the fixed per-get instrumentation cost ≤ 10% — the phase-boundary timestamps themselves, ~350–420 ns/get measured; the adoption-scale probe keeps the pure 10% where a ~36 µs get absorbs the same overhead as <2%), M21-02 memtable-hit leg (zero segment work — walk never starts), M21-03 cache-hit leg (no physical read; decode+cache dominant), M21-04 cache-miss leg (one read per get; io dominant), M21-05 merged-segment leg (the compaction output reader carries the shared cache + stats — the probe-found fix: the merge reopened its readers bare, so L1 reads were uncached AND uncounted). Probe `v2_attribution_probe` (opt-in `SE2M21_ATTRIB=1`, ~225 s) → `artifacts/storage-engine-v2/attribution.md`: W1 39.5 µs = engine 35.7 (block io 21.4 DOMINANT — 25K misses/20K ops, 406 MB read, 8 MiB cache thrashes; decode 3.1; bloom 2.6; index 2.4; cache 1.8; memtable 1.3; lock 0.1; residual 2.7 = 7.6% — hypotheses B sync and C metadata OUT) + kernel 3.8; head-row leg 4.8 µs (19,291/20K hits — the fix restored head-row caching); version row 20.0 µs; memtable-hit 0.7 / cache-hit 1.5 mechanism legs close |
| SE2-M22 | Fix the dominant point-read cost (P0) | ✅ 0/0 new | Levers shipped (5 files, `d9e4887`, no push): first-touch checksum8 chains `hasher.update` over the two slices (one less full-block memcpy per first touch, byte-identical digest); the key bloom hash pair computed ONCE per get inside the first segment's probe timer, shared by every segment's probe (was one sha256 per segment — m21_01/m21_05 accounting pins still close within 10%). Probe re-run (`attribution.md` regenerated): W1 P50 39.5 → 33.5 µs (−15% — the planned 25% was not reached), bloom 2.6 → 1.7 µs; block io 18.7 µs = 62% of the engine get_wall is the bounded-RAM floor (positional read + soft sha256 ~10 µs/16 KiB; fast backends surveyed and rejected — 0.10 `compress` is an empty stub, `asm` ships GAS `.S` unbuildable on MSVC, `x86-sha` crashes non-SHA CPUs — documented in Cargo.toml). **Gate 5 amendment (2026-09-05, user decision): bound ≤2× → ≤8×** — the original bar was RAM-vs-RAM (v1's mirror pays zero disk by design); the measured 5.6–6.7× sits inside the re-bound with 1.2–1.5× headroom, and a real regression (≈13×+) still fails the gate. Recorded in `adoption-decision.md`. Full v2 suite green (149); correctness pins hold |
| SE2-M23 | Head lookup specialization | skipped — condition not met | Gated on M21 proving generic traversal is the W2 cost; M21/M22 proved the W2 cost is block io + the bounded-RAM miss floor, not traversal. M11 already passes the hot-head gate 16× without any HeadIndex, and under the re-bounded gate 5 (≤8×, 09-05) the W2 head row already passes (32.8 µs ≈ 5.7× v1). Reopen only if a future matrix shows W2 breaking the re-bound |
| SE2-M24 | Bounded KO object cache | skipped — condition not met | Gated on decode/object construction being proven expensive; M21/M22 disproved it — decode is 2.5 µs of a 33.5 µs warm get (7.6%), block io is the dominant phase. An object cache would only improve repeated single-key loops (M25's batch path covers the fan-out case). Reopen only if a future attribution shows decode/construction dominant |
| SE2-M25 | Relationship batch read path | ✅ 3/3 new — falsified for W4 | `multi_get` (3): `multi_get_answers_match_loop` (350-key mix — memtable/segment/tombstone/absent/duplicate, `get_many` byte-identical to loop-of-get), `multi_get_one_lookup_per_unique_target` (8 positions / 4 unique → `lookups == 4`, duplicate positions share the first-position answer), `multi_get_kernel_matches_loop` (30 KOs, batch == per-target `k.get`, NotFound fail-fast parity). Shipped: `StorageEngine::get_many` defaulted trait method (= loop of get — memory/redb/v1/encrypting wrappers untouched; only v2 overrides) → `SegmentReader::get_many` (locate once, group by block, one fetch+checksum per block, per-key interval decode) → `Db::get_many` (dedup, one state guard) → `ObjectManager`/`Kernel::get_many` (one auth guard). Certification probe `v2_m25_relationship_batch` (`SE2M25_NIGHTLY=1`, release → `relationship-batch.md`): batch-vs-loop P50 0.73×–1.13× across runs — the sign flips with run-to-run noise, no measurable win at W4's warm fan-out shape; both suggested targets MISSED every run (F=100 788–1019 vs 700 µs, F=1000 10101–12093 vs 6000 µs); counters show decode is per-key in both shapes and the loop control inherits the batch leg's warmed cache. Keep-or-revert verdict: harness W4 legs stay on the per-target loop; the batch API remains, correctness-pinned (M27 may still evaluate it, with this falsification as prior evidence). Full v2 suite green |
| SE2-M26 | Type scan investigation | ✅ 0/0 new — index question answered, no index | Probe `v2_m26_scan_profile` (`SE2M26_NIGHTLY=1`, release → `type-scan-profile.md`). Discovery first: the W5 index shape is polluted — the harness's phase-2 `rmv(.., "m7_0")` restates every KO to m7_0, so m7_0 = 100,000 live candidates and m7_1..99 = 1000 stale phase-1 entries each, rejected by the payload re-check after full decode (stale entries kept by design, kernel.rs:1282); mean candidates/op = 1990. The probe reproduces the matrix cell: rotating p50 27.0 ms vs the 09-05 cell 27.5 ms (1.5%) — the cell IS the 99% stale-type op (1000 candidates → 0 returned); the 1% m7_0 op costs 1.35 s (invisible to p99, 35% of the mean). Decomposition (mean op): engine prefix scan 1.6%, engine point gets 77.8% (2 per candidate), kernel residual 20.6% (4.3 µs/candidate vs 3.2 µs for a plain k.get, +32%). Decision tree (thresholds pre-fixed): scan share 1.6% < 15% → no type index / no posting lists / no block summaries; kernel residual 20.6% ≤ 30% → no kernel instrumentation. W5 is candidate-bound (per-candidate head_object), not scan-bound; its warm gate-5 cell (4.96× v1) sits inside the amended ≤8× bound. M26 closes as a skip (M23/M24 pattern); harness shape unchanged (matrix cells are the certification reference) |
| SE2-M27 | Context compilation batch retrieval | ✅ 0/0 new — mix proven to differ, adoption question to review | Probe `v2_m27_context_profile` (`SE2M27_NIGHTLY=1`, release → `context-profile.md`). Reproduction: replaying the harness's EXACT W7 draw sequence (`Xs(SEED ^ 0x27).below(100_000)`, 5000 draws, capture-pinned, hubs included) gives L1 p50 219 µs vs the 09-05 cell 222 µs (−1.4%); a stride sample rode the ring's block locality and understated it by 30% — probe lesson: replay the harness's deterministic rng, never substitute a stride; post-thrash L6 207 µs (regime-insensitive). Decomposition of the 257 µs mean op: engine scans 23.6% (61 µs — the tree's block-summary band: scan-shape investigation opens), engine gets 54.8% (141 µs/op), kernel residual 21.6% (≤30% → no kernel instrumentation), history 16.0% (≤30% → no versions-path work). Batch (the M27 gate): 0.81×/0.87×/0.85× across three batch-vs-loop pairs in two runs — direction stable, never parity; mechanism pinned: segs/op 14 (batch) vs 116 (loop) — get_many resolves the segment list once, the loop re-walks it per get (M25's warm repeated-target shape hid this cost). The mix DOES differ from M25's falsified shape — a real ~13% p50 win on the get sub-shape (~7% of the op: 222 → ~207 µs) — but all three ratios sit in the pre-fixed 0.80–0.90 band (below the <0.80 reopen bar): no new primitives shipped (versions_many/relationships_many would batch per-row decode — history and edges are already ONE scan each); harness W7 legs stay on the per-target loop (matrix cells are the certification reference); adopting get_many in the harness = user decision at adoption review (W7 ~3.9× → ~3.6×, matrix re-run required) |
| SE2-M29 | Strong identity types (spec §31 M1) | ✅ 11/11 new | `identity_types` (11): equality + hash for all six (§6/§28.1) — ObjectId/LogicalId/ReplicaId/NodeId/SegmentId/BlockId; `ids_order_documenting_the_ordering_requirement` (u64 ids order by value, ObjectId byte-lexicographic — the §6.1 documented requirement); `distinct_types_even_with_equal_representation` (LogicalId/ReplicaId/NodeId/SegmentId = 8 B, BlockId = 4 B, ObjectId = 16 B while every `TypeId` differs — distinctness is type-level, not size-level; LogicalId(42) ≠ ReplicaId(42)); byte-exact persistence round-trips + golden pins (`object_id_persists_byte_exactly` 16 raw bytes, `u64_ids_persist_byte_exactly_little_endian`, `segment_and_block_ids_persist_byte_exactly`); `object_id_display_is_lowercase_hex` (`Display` = lowercase hex, `Debug` = `ObjectId({hex})`). ID-004 substitution-impossible as a `compile_fail` doc-test on the `identity` module (the §6.3 example verbatim: `accepts_replica(logical)` must not compile — runs with `cargo test --doc`). No engine code touched — the existing suite (incl. the 3-min M7 matrix) stayed green |
| SE2-M30 | Identity directory (spec §32 M2) | ✅ 6/6 new | `identity_directory` (6): id010 `create_object` allocates + resolves (distinct oid/lid; GroupCommit committer arm exercised); id011 stable resolution, unknown ObjectId → None; id012 restart splits both recovery paths (5 flushed creates via IDENTITY-{gen:06}.log, 5 unflushed via WAL replay — all 10 resolve); id013 500 creates → 500 distinct lids; id014 monotonic allocator across restart (reopened create gets lid > max); id015 child-kill parks at `after_identity` mid-flush — CURRENT gen 1 while IDENTITY-000002.log/REPLICA-000002.log exist, reopen resolves the child's mapping exactly once, next create gets lid 2 (gap burned, not reused), orphan log stays reported-and-ignored. WAL op byte 3 `CreateObject` (fixed `oid 16 | lid 8 | rid 8`, no length prefixes); ops 1/2 byte-identical — wal_golden held |
| SE2-M31 | Local replica directory + topology (spec §33 M3) | ✅ 5/5 new | `identity_topology` (5): rp001 32 creates resolve 1:1 to distinct local replicas, `replicas_for` = exactly one descriptor on LOCAL_NODE_ID; rp002 stable across independently constructed views, directory/topology surfaces agree; rp003 restart splits both recovery sources (3 flushed delta-log + 2 WAL-replay — all hold); rp004 allocator resumes past observed max across restart, nothing reused; rp005 TypeId-distinct at equal size + Send/Sync trait bound on both MVP views. Shape: `identity/topology.rs` (§9.2/§10 traits, FormatError per §27; views borrow the live Db) |
| SE2-M32 | Placement directory (spec §34 M4) | ✅ 9/9 new | `placement_directory` (9): pl001 Memtable + Segment placements resolve; pl002 unknown ReplicaId → None; pl003 restart splits both recovery sources (3 flushed delta-log + 2 WAL-replay — all still `Memtable` per §14); pl004 atomic replace leaves one entry; pl005 full gate matrix (Stale ignored / Duplicate no-op / Applied replaces / equal-gen-different fails closed `Corrupt`); format contracts — all-variant round-trip with the 125-byte size pin (18 + 3×33 + 8 — the pin caught a real encode bug: non-Segment records zero-filled 20 bytes instead of the 16 placement-field bytes), damage classes (magic/version/truncation/trailing/checksum), publish→recover through the caller's validation closure, `validate_segment_location` (manifest membership + block/entry range). `CreateObject` payload grew 32→40 bytes with `pgen` (replay reproduces the exact placement record — the §24 state-D fix); ops 1/2 byte-identical, wal_golden held |
| SE2-M33 | Memtable integration (spec §35 M5) | ✅ 9/9 new | `memtable_identity` (9): mt001 put-to-unknown-ObjectId IS the create (distinct identities for distinct objects, stable across re-puts); mt002/003/004 update/delete/re-write preserve ObjectId/LogicalId/ReplicaId (delete keeps the directory entries, §16); mt005 put/update/delete cycle via `get_object` + the §11 filter pin (a newer byte-API row at the same key never answers the object read); mt006 WAL-only restart replays rid-carrying entries; mt007 `get_object` past flush fails closed `Unsupported` (SE2-M34 lifted this boundary — the row now reads the flushed segment, see the M34 row); mt008 GC committer applies object ops; mt009 ops 4/5 round-trip, byte-API frames byte-identical (wal_golden held). `MemEntry.replica_id` (0 = byte API); ops 4/5 carry `rid u64` after the op byte |
| SE2-M34 | Flush (spec §36 M6) | ✅ 12/12 new | `block_v3` (7): roundtrip (every read surface — get/versions/scan — returns the written rid, per entry not per key); matches_v2_for_zero_rids (rid-0 v3 ≡ v2 on every surface: scan/get/versions/streaming iter); anchors_point_at_each_rids_head (per-rid anchor = the max-seq entry, byte-API rows never anchor, `publish()` writes exactly `publish_with_anchors`' bytes); anchors_span_blocks (block-local ids/offsets verified via `entry_at` over every block); lookup_with_rid_filter_is_bounded (10k-entry block, §11 filter — a byte-API row never answers the object read, ≤2×16 entries decoded); entry_at_bounds (past block/segment → None); golden (python-computed v3 fixture — header/index/bloom checksums identical to the v2 golden, only the data block differs). `flush_identity` (5): fl001 memtable→segment preserves ReplicaId (v3 blocks carry the rid, distinct objects never share); fl002 placement resolves to the anchor — `entry_at(loc.block_id, loc.entry_offset)` yields the object's max-seq entry with its rid; fl003 Memtable placement not authoritative after publication + survives restart (the PLACEMENT log rode the publication window); fl004 child-kill at `after_identity` — the unpublished flush never moves the placement (§24 state-B, WAL replays Memtable); fl005 compact with Segment placements fails closed `Unsupported` (the M35 boundary — the test caught the ≤1-segment pre-check gap in `compact()`; SE2-M35 flipped this pin to the relocation assertion — see the M35 row). `memtable_identity` mt007 LIFTED: `get_object` answers through the v3 segment path (rid-filtered bounded decode) and the §11 filter holds on disk. Byte-API flushes stay v2 — `block_v2_golden`, `segment_golden`, `v2_publish_pin` held |
| SE2-M35 | Compaction relocation (spec §37 M7) | ✅ 10/10 new | `compaction_relocation` (10): cp001..003 compaction preserves the identity triple — get_object answers all five keys, the directory resolves all three oids (cp001), lid/rid stable across the merge (cp002/003); cp004 the location MAY change — all three pre-compact placements are Segments, every post-compact placement names a FRESH segment (never an input) whose anchor resolves with the matching rid; cp005 the relocation = the max-seq surviving entry (a3/va3/rid) and survives restart; cp006 the old location never returns — new ≠ old, the obsolete segment file retired, stable across 2 restarts; cp007 tombstone-dropped replica → `Retired` (§16 — identity still resolves, Retired survives restart); cp008 generations monotonic across two compact cycles (§25); cp009 child-kill at `after_location` (state-C): PLACEMENT-{CURRENT+1}.log exists, reopened placements = the pre-compact ones exactly, data readable; cp010 child-kill at `after_current` (state-D): reopened placements = the relocated ones exactly. `flush_identity` fl005 LIFTED: with Segment placements present, compact relocates instead of refusing (the M34 guard pin flipped — anchor resolves to k2/v2, old segment retired). Relocation set → PLACEMENT-{gen+1}.log → manifest → CURRENT → retire (the §23 ordering) |
| SE2-M36 | Crash injection (spec §38 M8) | ✅ 7/7 new | `AIKOQL_V2_PLACE_PARK` FAIL_AFTER_SEGMENT_WRITE/SEGMENT_FSYNC/LOCATION_WRITE/LOCATION_FSYNC/MANIFEST_WRITE/MANIFEST_FSYNC/PUBLISH — 7 windows × child-kill (KSE-15 harness) → reopen → validate consistency → verify all objects → verify no invalid locations; §24 states A–D fingerprint-pinned; staged publishers (`publish_atomic_writer_staged`) park at the write/fsync boundaries — compaction path only, flush stays unstaged |
| SE2-M37 | Recovery (spec §39 M9) | ✅ 5/5 new | RC-001..005: restart restores identity mappings; restores replica mappings; restores placement mappings; all committed objects readable (incl. the memtable-only committed object via WAL replay); no uncommitted relocation visible — state-C orphan across restart + second compaction on top of the orphan succeeds with strictly newer §25 generations, final restart keeps them |
| SE2-M38 | Relocation stress + memory (spec §40 M10 + §45) | ✅ 2/2 new | `identity_stress` (2): st001 always-on 500-object smoke (4 flush batches, every-7th update, flush, compact, 2 restarts × identity-triple/value/placement verification); `SE2M38_NIGHTLY=1` → 100k (§40 — ObjectId/LogicalId/ReplicaId pinned unchanged, every committed value byte-exact, sampled placements resolve to rid-carrying entries) + Q5 amplification report (compact stats + directory-log records/bytes vs live bytes). mem001 §45 memory gate (1M objects, WorkingSet64 plateaus at half/full markers, per-directory capacity-exact resident bytes, bytes/object MEASURED and reported — never asserted). The stress RED-caught two engine bugs (probe_m38 diagnosis, deleted): the merge's per-key dedupe destroyed every non-newest same-key row in the rid space (fix: per-(key,rid) winners — compaction.rs), and the v2 writer's equal-key restart skip landed a mid-run restart + `get_by_rid` never crossed block boundaries, hiding a hot key's run-head rows (fix: restart repositioned to the run head, reader falls through straddled blocks — segment.rs). Numbers → `artifacts/storage-engine-v2/identity-placement.md` |
| SE2-M39 | Randomized oracle + perf certification (spec §41 M11 + §43/44) | planned | Random PUT/UPDATE/DELETE/FLUSH/COMPACT/RESTART/CRASH/RECOVER vs an independent `BTreeMap<ObjectId, ExpectedState>` oracle (never AIKOQL-internal abstractions); W1..W8 matrix re-run (byte API unchanged — hot/warm ≤10%, cold ≤15% gates ride the same harness); new identity metrics reported (lookup latency P50, directory RSS, persistence size); gate failures get the §13 evidence pack |
| Scale | Mandatory scale experiment (1M / 10M) | planned | 100K is fully measured (09-01 cold + 09-04 warm matrices, W1–W8 + resources). 1M has M14 QA-gate cells only (DS-PERF-L: hot head 2.4 µs, cold 30.1 µs — no W1–W8 matrix); 10M never measured. Run W1–W8 at both after SE2-M22; answer: at what scale, if any, does v2 become competitive with v1 |

## Rules carried from v1

- Strict opt-in envs: every nightly-sized variant (`SE2M*_NIGHTLY`) panics on any value other than documented ones.
- Evidence preservation: test name, build mode, dataset, environment, results, PASS/FAIL, known limitations — in the artifact, per suite.
- Correctness pins are asserts; performance numbers are report cells. A perf number never gates a test.
- Report writers regenerate their artifact only when run with the env set; never re-run a smoke after a nightly (artifact churn, M7 lesson).
- RSS measurement is Windows-only (PowerShell WorkingSet64 child sampler); non-Windows rows = honest NOT_SAMPLED cells.
- Child-kill recovery tests spawn `current_exe --exact <test>` with env gates (no args = the whole suite — KSE-15 lesson).
